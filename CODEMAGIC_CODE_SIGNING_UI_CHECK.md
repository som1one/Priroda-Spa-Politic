# 🔍 Проверка настроек Code Signing в UI Codemagic

## 🚨 Проблема

Codemagic не использует настройки code signing из UI. В логах видно:
- `Method: ad-hoc` (должно быть `app-store`)
- `Team Id: ` (пустое)
- `Signing Style: manual` (должно быть `automatic`)
- `Archiving com.example.spa...` (неправильный Bundle ID)

---

## ✅ Решение: Проверьте настройки Code Signing в UI

### Шаг 1: Откройте настройки Workflow

1. **Codemagic → Ваш проект → Workflows**
2. Откройте ваш **iOS workflow** (который вы используете)
3. Найдите вкладку **"Code signing"** или **"iOS code signing"**

---

### Шаг 2: Проверьте настройки

Убедитесь, что:

1. **Метод подписи:**
   - ✅ Должно быть: **"Automatic"** (не Manual)
   - ❌ Если "Manual" - переключите на "Automatic"

2. **App Store Connect API key:**
   - ✅ Должен быть выбран: **"Priroda Spa"** или **"Priroda Spa (Key: 84SR375827)"**
   - ❌ Если пусто или "None" - выберите ключ из списка

3. **Bundle identifier:**
   - ✅ Должно быть: `com.prirodaspa.app`
   - ❌ Если `com.example.spa` или пусто - введите `com.prirodaspa.app`

4. **Provisioning profile type:**
   - ✅ Должно быть: **"App store"** (не Development или Ad hoc)
   - ❌ Если другое - измените на "App store"

5. **Team ID:**
   - ✅ Может быть пустым (Codemagic определит автоматически через API ключ)
   - Или введите ваш Team ID из Apple Developer Portal

---

### Шаг 3: Сохраните настройки

1. После проверки всех настроек нажмите **"Save"** или **"Save workflow"**
2. Убедитесь, что изменения сохранены

---

## 🔍 Если настройки правильные, но не работают

### Вариант 1: Создайте новый Workflow через UI

1. **Codemagic → Workflows → "+" → Create workflow**
2. Выберите **"iOS"**
3. Настройте:
   - **Working directory:** `spa`
   - **Code signing:** Automatic
   - **App Store Connect API key:** "Priroda Spa"
   - **Bundle identifier:** `com.prirodaspa.app`
   - **Provisioning profile type:** App store
4. **Save**
5. Запустите сборку с новым workflow

### Вариант 2: Проверьте, что используется правильный Workflow

1. При запуске сборки убедитесь, что выбран **правильный workflow**
2. Не используйте "Default Workflow" - создайте новый или используйте из yaml

---

## ⚠️ Важно: Bundle ID в логах

В логах видно `Archiving com.example.spa...`, но в проекте Bundle ID правильный (`com.prirodaspa.app`).

**Возможные причины:**
1. Codemagic использует дефолтный Bundle ID
2. Настройки code signing не применены к workflow
3. Workflow не использует настройки из UI

**Решение:**
- Явно укажите Bundle ID в настройках workflow: `com.prirodaspa.app`
- Убедитесь, что настройки code signing применены к workflow

---

## 📝 Чек-лист перед запуском

Перед запуском сборки убедитесь:

- [ ] **Code signing: Automatic** (в настройках workflow)
- [ ] **App Store Connect API key:** "Priroda Spa" (выбран)
- [ ] **Bundle identifier:** `com.prirodaspa.app` (указан явно)
- [ ] **Provisioning profile type:** App store
- [ ] **Настройки сохранены** (нажали Save)
- [ ] **Используется правильный workflow** (не Default Workflow)

---

## 🎯 Рекомендуемое решение

### Создайте новый Workflow через UI:

1. **Codemagic → Workflows → "+" → Create workflow**
2. **Тип:** iOS
3. **Настройки:**
   ```
   Working directory: spa
   Flutter version: stable
   Xcode version: latest
   
   Code signing:
     Method: Automatic
     App Store Connect API key: Priroda Spa
     Bundle identifier: com.prirodaspa.app
     Provisioning profile type: App store
   
   Build arguments: --release
   ```
4. **Save**
5. Запустите сборку

---

## 🔧 Альтернатива: Исправить yaml

Если хотите использовать yaml, нужно убедиться, что:
- Настройки code signing в UI правильные
- Workflow использует эти настройки (но это не всегда работает)

**Рекомендация:** Используйте UI workflow для iOS code signing!

---

**Главное:** Проверьте настройки code signing в UI workflow и убедитесь, что они правильно применены. Если не работает - создайте новый workflow через UI!

