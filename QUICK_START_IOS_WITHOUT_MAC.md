# 🚀 Быстрый старт: iOS сборка БЕЗ Mac и БЕЗ сертификатов

## ✅ Самый простой способ (рекомендуется)

Используйте **App Store Connect API** - не нужны сертификаты, не нужен Mac!

---

## 📋 Шаг 1: Создайте API ключ в App Store Connect

1. Откройте [appstoreconnect.apple.com](https://appstoreconnect.apple.com)
2. **Users and Access** → **Keys** → **"+"**
3. **Name:** `GitHub Actions` (любое имя)
4. **Access:** `App Manager` (или `Admin`)
5. **Generate** → **Download** (`.p8` файл)
6. **Скопируйте:**
   - **Issuer ID** (например: `4fbfcedf-2756-4b8e-8fc3-b17978e9532a`)
   - **Key ID** (например: `84SR375827`)

⚠️ **Важно:** `.p8` файл скачивается только один раз! Сохраните его.

---

## 🔐 Шаг 2: Добавьте в GitHub Secrets

1. GitHub → ваш репозиторий → **Settings** → **Secrets and variables** → **Actions**
2. **New repository secret** → добавьте 3 секрета:

### `APP_STORE_ISSUER_ID`
- **Value:** Issuer ID (например: `4fbfcedf-2756-4b8e-8fc3-b17978e9532a`)

### `APP_STORE_KEY_ID`
- **Value:** Key ID (например: `84SR375827`)

### `APP_STORE_PRIVATE_KEY`
- **Value:** Откройте `.p8` файл в текстовом редакторе
- Скопируйте **весь текст**, включая:
  ```
  -----BEGIN PRIVATE KEY-----
  ...ваш ключ...
  -----END PRIVATE KEY-----
  ```

---

## 🚀 Шаг 3: Запустите сборку

1. GitHub → **Actions**
2. Выберите **"iOS Build (App Store Connect API)"**
3. **Run workflow** → выберите ветку (`main`) → **Run workflow**

**Готово!** Через несколько минут IPA файл будет готов.

---

## 📦 Шаг 4: Скачайте IPA

1. После успешной сборки откройте workflow run
2. Прокрутите вниз до **"Artifacts"**
3. Нажмите **"ios-ipa-api"**
4. Скачайте `.ipa` файл

---

## ⚠️ Важно

1. **Bundle ID должен быть зарегистрирован:**
   - [developer.apple.com/account](https://developer.apple.com/account) → Identifiers
   - Должен быть: `com.prirodaspa.app`

2. **API ключ должен иметь правильные права:**
   - `App Manager` или `Admin` (не `Developer`)

3. **`.p8` файл храните в безопасности:**
   - Не загружайте в публичные репозитории
   - Используйте только GitHub Secrets

---

## 🐛 Если не работает

### Ошибка: "No valid code signing certificates were found"

**Решение:**
- Проверьте, что все 3 секрета добавлены правильно
- Убедитесь, что `.p8` файл скопирован полностью (включая `-----BEGIN PRIVATE KEY-----` и `-----END PRIVATE KEY-----`)

### Ошибка: "Bundle identifier not found"

**Решение:**
- Зарегистрируйте Bundle ID в Apple Developer Portal
- Identifiers → "+" → App IDs → `com.prirodaspa.app`

---

**Это самый простой способ! Не нужен Mac, не нужны сертификаты - все работает автоматически через API.**

