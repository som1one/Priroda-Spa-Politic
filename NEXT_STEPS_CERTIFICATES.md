# 📋 Пошаговый гайд: Следующие шаги после создания CSR

## ✅ Что уже готово:
- ✅ `CertificateSigningRequest.certSigningRequest` - создан
- ✅ `private_key.pem` - создан
- ✅ `.gitignore` - настроен

---

## 🔐 Шаг 1: Загрузите CSR в Apple Developer Portal

### 1.1. Откройте Apple Developer Portal

1. Перейдите на [developer.apple.com/account](https://developer.apple.com/account)
2. Войдите в свой аккаунт разработчика
3. Нажмите **"Certificates, Identifiers & Profiles"**

### 1.2. Создайте Distribution Certificate

1. В левом меню выберите **"Certificates"**
2. Нажмите кнопку **"+"** (вверху справа)
3. Выберите **"iOS Distribution"** → **"App Store and Ad Hoc"**
4. Нажмите **"Continue"**

### 1.3. Загрузите CSR файл

1. Нажмите **"Choose File"** или **"Выбрать файл"**
2. Найдите файл: `D:\PycharmProjects\Spa\CertificateSigningRequest.certSigningRequest`
3. Выберите файл → **"Open"** / **"Открыть"**
4. Нажмите **"Continue"**

### 1.4. Скачайте сертификат

1. После обработки нажмите **"Download"** / **"Скачать"**
2. Сохраните файл `ios_distribution.cer` (или похожее имя) в папку проекта: `D:\PycharmProjects\Spa\`

**Важно:** Переименуйте файл в `certificate.cer` для удобства.

---

## 🔄 Шаг 2: Конвертируйте .cer в .p12

### 2.1. Откройте PowerShell в папке проекта

```powershell
cd D:\PycharmProjects\Spa
```

### 2.2. Конвертируйте .cer в .pem

```powershell
openssl x509 -inform DER -in certificate.cer -out certificate.pem
```

**Если файл называется по-другому, замените `certificate.cer` на имя вашего файла.**

### 2.3. Создайте .p12 файл

```powershell
# Замените YOUR_PASSWORD на свой пароль (запомните его!)
openssl pkcs12 -export -out certificate.p12 -inkey private_key.pem -in certificate.pem -password pass:YOUR_PASSWORD
```

**Пример:**
```powershell
openssl pkcs12 -export -out certificate.p12 -inkey private_key.pem -in certificate.pem -password pass:MySecurePass123
```

**Важно:** 
- Запомните пароль! Он понадобится для GitHub Secrets
- Пароль должен быть надежным (минимум 8 символов)

### 2.4. Проверьте создание .p12

```powershell
Test-Path certificate.p12
```

Должно вернуть `True`.

---

## 📱 Шаг 3: Создайте Provisioning Profile

### 3.1. Откройте раздел Profiles

1. В Apple Developer Portal → **"Certificates, Identifiers & Profiles"**
2. В левом меню выберите **"Profiles"**
3. Нажмите **"+"** (вверху справа)

### 3.2. Выберите тип профиля

1. Выберите **"App Store"** (для публикации в App Store)
2. Нажмите **"Continue"**

### 3.3. Выберите App ID

1. В списке найдите и выберите: **`com.prirodaspa.app`**
   - Если его нет → создайте: **Identifiers** → **"+"** → **App IDs** → **App** → введите `com.prirodaspa.app`
2. Нажмите **"Continue"**

### 3.4. Выберите сертификат

1. Выберите **Distribution Certificate** (созданный в Шаге 1)
2. Нажмите **"Continue"**

### 3.5. Назовите профиль

1. **Provisioning Profile Name:** `Priroda Spa App Store` (любое имя)
2. Нажмите **"Generate"**

### 3.6. Скачайте профиль

1. После создания нажмите **"Download"**
2. Сохраните файл `Priroda_Spa_App_Store.mobileprovision` (или похожее имя) в папку проекта: `D:\PycharmProjects\Spa\`

**Важно:** Переименуйте файл в `profile.mobileprovision` для удобства.

---

## 🔐 Шаг 4: Конвертируйте в Base64 для GitHub

### 4.1. Конвертируйте .p12 в base64

```powershell
[Convert]::ToBase64String([IO.File]::ReadAllBytes("certificate.p12")) | Out-File -Encoding ASCII certificate_base64.txt
```

### 4.2. Конвертируйте .mobileprovision в base64

```powershell
[Convert]::ToBase64String([IO.File]::ReadAllBytes("profile.mobileprovision")) | Out-File -Encoding ASCII profile_base64.txt
```

### 4.3. Проверьте создание файлов

```powershell
Test-Path certificate_base64.txt, profile_base64.txt
```

Оба должны вернуть `True`.

### 4.4. Откройте файлы и скопируйте содержимое

```powershell
# Откройте certificate_base64.txt
notepad certificate_base64.txt

# Откройте profile_base64.txt
notepad profile_base64.txt
```

**Скопируйте весь текст** из каждого файла (включая все символы).

---

## 🔑 Шаг 5: Добавьте секреты в GitHub

### 5.1. Откройте GitHub Secrets

1. Перейдите на GitHub → ваш репозиторий
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**

### 5.2. Добавьте секреты (по очереди):

#### Секрет 1: `APPLE_CERTIFICATE_BASE64`
- **Name:** `APPLE_CERTIFICATE_BASE64`
- **Secret:** Вставьте весь текст из `certificate_base64.txt`
- **Add secret**

#### Секрет 2: `APPLE_CERTIFICATE_PASSWORD`
- **Name:** `APPLE_CERTIFICATE_PASSWORD`
- **Secret:** Пароль, который вы использовали при создании `.p12` (в Шаге 2.3)
- **Add secret**

#### Секрет 3: `APPLE_PROVISIONING_PROFILE_BASE64`
- **Name:** `APPLE_PROVISIONING_PROFILE_BASE64`
- **Secret:** Вставьте весь текст из `profile_base64.txt`
- **Add secret**

#### Секрет 4: `KEYCHAIN_PASSWORD` (опционально)
- **Name:** `KEYCHAIN_PASSWORD`
- **Secret:** Любой пароль (например, `temp_password`)
- **Add secret**

**Проверьте:** Должно быть 4 секрета:
- ✅ `APPLE_CERTIFICATE_BASE64`
- ✅ `APPLE_CERTIFICATE_PASSWORD`
- ✅ `APPLE_PROVISIONING_PROFILE_BASE64`
- ✅ `KEYCHAIN_PASSWORD` (опционально)

---

## 🚀 Шаг 6: Запустите сборку

### 6.1. Откройте GitHub Actions

1. GitHub → ваш репозиторий → вкладка **"Actions"**
2. В левом меню выберите **"iOS Build"** workflow

### 6.2. Запустите workflow вручную

1. Нажмите **"Run workflow"** (справа)
2. Выберите ветку: **`main`** (или `master`)
3. Нажмите **"Run workflow"**

### 6.3. Дождитесь завершения

- Сборка займет 5-15 минут
- Можно следить за прогрессом в реальном времени
- При успехе появится зеленый галочка ✅

---

## 📦 Шаг 7: Скачайте IPA файл

### 7.1. Откройте завершенный workflow

1. После успешной сборки откройте workflow run
2. Прокрутите вниз до раздела **"Artifacts"**

### 7.2. Скачайте IPA

1. Нажмите на **"ios-ipa"**
2. Скачайте `.ipa` файл

**Готово!** Теперь у вас есть готовый IPA файл для загрузки в App Store Connect.

---

## ⚠️ Важные моменты

### Безопасность:
1. **Не коммитьте** файлы сертификатов в Git (они уже в `.gitignore`)
2. **Не делитесь** `.p12` и `.mobileprovision` файлами
3. **Храните** пароль от `.p12` в безопасном месте

### После использования:
1. Можно удалить временные файлы:
   ```powershell
   Remove-Item certificate.pem, certificate_base64.txt, profile_base64.txt
   ```
2. **НЕ удаляйте:**
   - `certificate.p12` (может понадобиться)
   - `profile.mobileprovision` (может понадобиться)
   - `private_key.pem` (может понадобиться для обновления сертификата)

### Если что-то пошло не так:

**Ошибка при конвертации .cer:**
- Проверьте, что файл называется `certificate.cer`
- Убедитесь, что OpenSSL установлен и в PATH

**Ошибка при создании .p12:**
- Проверьте, что `private_key.pem` и `certificate.pem` в той же папке
- Убедитесь, что используете правильный пароль

**Ошибка в GitHub Actions:**
- Проверьте, что все 4 секрета добавлены правильно
- Убедитесь, что base64 текст скопирован полностью (без пробелов в начале/конце)

---

## 📝 Чеклист выполнения

- [ ] CSR загружен в Apple Developer Portal
- [ ] `.cer` файл скачан
- [ ] `.cer` конвертирован в `.pem`
- [ ] `.p12` файл создан с паролем
- [ ] Provisioning Profile создан и скачан
- [ ] `.p12` конвертирован в base64
- [ ] `.mobileprovision` конвертирован в base64
- [ ] `APPLE_CERTIFICATE_BASE64` добавлен в GitHub Secrets
- [ ] `APPLE_CERTIFICATE_PASSWORD` добавлен в GitHub Secrets
- [ ] `APPLE_PROVISIONING_PROFILE_BASE64` добавлен в GitHub Secrets
- [ ] `KEYCHAIN_PASSWORD` добавлен в GitHub Secrets
- [ ] Workflow запущен
- [ ] IPA файл скачан

---

**Готово!** Следуйте шагам по порядку, и все получится! 🎉

