# Полный гайд по настройке YClients интеграции

## 📋 Содержание

1. [Что уже реализовано](#что-уже-реализовано)
2. [Настройка окружения](#настройка-окружения)
3. [Настройка YClients API](#настройка-yclients-api)
4. [Настройка Webhook](#настройка-webhook)
5. [Тестирование](#тестирование)
6. [Troubleshooting](#troubleshooting)
7. [Проверка работы](#проверка-работы)

---

## ✅ Что уже реализовано

### Backend

1. **Сервис YClients** (`backend/app/services/yclients_service.py`)
   - Получение услуг (`get_services`)
   - Получение мастеров (`get_staff`)
   - Получение расписания (`get_staff_with_schedule`)
   - Получение доступных дат (`get_available_dates`)
   - Создание записи (`create_booking`)
   - Отмена записи (`cancel_booking`)
   - Получение записей (`get_bookings`)

2. **API Endpoints** (`backend/app/api/v1/yclients.py`)
   - `GET /api/v1/yclients/widget-url/{service_id}` - получение URL формы записи
   - `POST /api/v1/yclients/sync-bookings` - ручная синхронизация записей
   - `POST /api/v1/yclients/webhook` - webhook от YClients

3. **Синхронизация записей** (`backend/app/services/booking_sync_service.py`)
   - Периодическая синхронизация каждые 30 минут
   - Поиск пользователя по уникальному коду, email, телефону
   - Автоматическое создание/обновление записей в БД
   - Начисление бонусов за завершенные записи

4. **Уникальный код пользователя**
   - Поле `unique_code` в таблице `users`
   - Автоматическая генерация при первом запросе формы
   - Добавление кода в комментарий формы YClients

### Flutter

1. **Экран записи через YClients** (`spa/lib/screens/booking/yclients_booking_screen.dart`)
   - WebView для отображения формы YClients
   - Определение успешной записи
   - Навигация после записи

2. **Отображение кода в профиле** (`spa/lib/screens/profile/profile_screen.dart`)
   - Бейдж с уникальным кодом
   - Иконка копирования

### Admin Panel

1. **Поиск по коду** (`admin/src/pages/UsersPage.jsx`)
   - Поиск пользователя по уникальному коду
   - Списание бонусов по коду

---

## 🔧 Настройка окружения

### 1. Обновите `.env` файл

Откройте `backend/.env` и убедитесь, что есть следующие настройки:

```env
# YClients настройки
YCLIENTS_ENABLED=True
YCLIENTS_COMPANY_ID=235564
YCLIENTS_API_TOKEN=ваш-api-token
YCLIENTS_USER_TOKEN=ваш-user-token

# URL формы записи (опционально, но рекомендуется)
YCLIENTS_BOOKING_FORM_URL=https://n239661.yclients.com
YCLIENTS_NETWORK_FORM_URL=https://n328197.yclients.com

# База данных (порт 5437)
DATABASE_URL=postgresql://user:password@localhost:5437/spa_db
```

### 2. Получение токенов YClients

#### API Token (Partner Token)

1. Перейдите в YClients: **Настройки → Интеграции → API**
2. Найдите раздел **"Partner Token"**
3. Скопируйте токен
4. Вставьте в `.env` как `YCLIENTS_API_TOKEN`

#### User Token

1. Перейдите в YClients: **Настройки → Интеграции → API**
2. Найдите раздел **"User Token"**
3. Если токена нет, создайте его:
   - Нажмите "Создать токен"
   - Выберите права доступа (минимум: чтение записей, создание записей)
4. Скопируйте токен
5. Вставьте в `.env` как `YCLIENTS_USER_TOKEN`

**Важно:** User Token должен иметь права на:
- ✅ Чтение услуг (`GET /services`)
- ✅ Чтение мастеров (`GET /staff`)
- ✅ Чтение записей (`GET /records`)
- ✅ Создание записей (`POST /records`)
- ✅ Отмена записей (`DELETE /records`)

### 3. Проверка настроек

Запустите скрипт проверки:

```bash
cd backend
python scripts/check_yclients_config.py
```

**Ожидаемый результат:**
```
✅ YCLIENTS_ENABLED = True
✅ YCLIENTS_COMPANY_ID = 235564
✅ YCLIENTS_API_TOKEN = ...
✅ YCLIENTS_USER_TOKEN = ...
✅ Подключение успешно! Найдено X услуг
```

**Если скрипта нет, используйте этот код:**

Скрипт уже существует: `backend/scripts/check_yclients_config.py`

---

## 🔗 Настройка Webhook

### 1. Получите публичный URL

Для локальной разработки используйте **ngrok**:

```bash
# Установите ngrok (если еще не установлен)
# https://ngrok.com/download

# Запустите ngrok
ngrok http 9003
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

### 2. Настройте Webhook в YClients

1. Перейдите в YClients: **Настройки → Интеграции → Webhooks**
2. Нажмите **"Добавить URL"**
3. Введите URL: `https://ваш-ngrok-url.ngrok.io/api/v1/yclients/webhook`
4. Выберите события:
   - ✅ **Создание записи** (`record.created`)
   - ✅ **Изменение записи** (`record.updated`)
   - ✅ **Отмена записи** (`record.deleted`)
5. Сохраните

### 3. Проверка Webhook

Создайте тестовую запись в YClients и проверьте логи бэкенда:

```bash
# Логи бэкенда
docker compose logs -f backend
```

Должно появиться сообщение:
```
Получен webhook от YClients: {...}
```

---

## 🧪 Тестирование

### 1. Тест получения услуг и мастеров

Используйте готовый скрипт синхронизации:

```bash
cd backend
python scripts/sync_yclients_catalog.py
```

**Ожидаемый результат:**
```
INFO Услуги синхронизированы: создано X, обновлено Y
INFO Мастера синхронизированы: создано X, обновлено Y
```

### 2. Тест синхронизации записей

```bash
# Через API (нужен токен авторизации)
curl -X POST "http://localhost:9003/api/v1/yclients/sync-bookings" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Или через скрипт
python scripts/sync_yclients_catalog.py
```

### 3. Тест создания записи через форму

1. Откройте приложение Flutter
2. Перейдите в меню
3. Нажмите "Записаться онлайн"
4. Заполните форму в YClients
5. Создайте запись
6. Проверьте, что запись появилась в БД:

```bash
# Проверка записей в БД
python scripts/check_booking_sync.py
```

Или через API:

```bash
curl "http://localhost:9003/api/v1/booking?upcoming_only=true" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 🔍 Troubleshooting

### Проблема: `401 Unauthorized` при запросах к YClients

**Причины:**
- Неверный API Token или User Token
- Токены содержат не-ASCII символы
- Токены пустые или с пробелами
- Неверный формат заголовков

**Решение:**
1. Проверьте токены в `.env` (без пробелов в начале/конце)
2. Убедитесь, что токены скопированы полностью
3. Проверьте права доступа User Token в YClients
4. Запустите проверку:
   ```bash
   python scripts/check_yclients_config.py
   ```

**Если проблема сохраняется:**
- Проверьте логи бэкенда на наличие ошибок кодировки
- Убедитесь, что токены в правильном формате (обычно это длинные строки)
- Попробуйте создать новый User Token в YClients

### Проблема: `403 Forbidden` при запросах к `/staff` или `/records`

**Причина:** User Token не имеет достаточных прав

**Решение:**
1. Перейдите в YClients: **Настройки → Интеграции → API**
2. Найдите ваш User Token
3. Проверьте права доступа
4. Если нужно, создайте новый токен с полными правами

### Проблема: Записи не синхронизируются

**Проверьте по порядку:**

1. **Настройки YClients:**
   ```bash
   python scripts/check_booking_sync.py
   ```
   Должно быть:
   - ✅ YCLIENTS_ENABLED: True
   - ✅ Токены установлены
   - ✅ Пользователей с кодом: X из X

2. **Периодическая синхронизация работает:**
   ```bash
   docker compose logs -f backend | grep -i "синхронизация\|sync"
   ```
   Должны быть сообщения каждые 30 минут:
   ```
   INFO Начало периодической синхронизации бронирований из YClients
   ```

3. **Webhook настроен (если используется):**
   - Проверьте URL в YClients
   - Убедитесь, что ngrok запущен (для локальной разработки)
   - Проверьте доступность: `curl https://ваш-url/api/v1/yclients/webhook`
   - Проверьте логи при создании записи в YClients

4. **Ручная синхронизация:**
   ```bash
   curl -X POST "http://localhost:9003/api/v1/yclients/sync-bookings" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

5. **Проверка записей в YClients:**
   - Убедитесь, что записи создаются в YClients
   - Проверьте, что в комментарии есть код: `"Код клиента: ABC12345"`
   - Проверьте email/телефон клиента в YClients

### Проблема: Пользователь не находится при синхронизации

**Причины:**
- В комментарии записи нет кода
- Email/телефон не совпадают
- Пользователь не имеет `unique_code`

**Решение:**

1. **Проверьте код в комментарии:**
   - Откройте запись в YClients
   - Проверьте комментарий - должно быть: `"Код клиента: ABC12345"`

2. **Проверьте данные пользователя:**
   ```sql
   SELECT id, email, phone, unique_code FROM users WHERE email = 'email@example.com';
   ```

3. **Сгенерируйте код для пользователя:**
   ```bash
   python scripts/generate_unique_codes.py
   ```

### Проблема: Бонусы не начисляются

**Проверьте:**

1. **Программа лояльности включена:**
   ```env
   LOYALTY_ENABLED=True
   ```

2. **Запись имеет статус COMPLETED:**
   - В YClients запись должна быть отмечена как "Завершена"
   - Статус в БД должен быть `completed`

3. **Бонусы еще не начислены:**
   ```sql
   SELECT id, status, loyalty_bonuses_awarded, loyalty_bonuses_amount 
   FROM bookings 
   WHERE status = 'completed' AND loyalty_bonuses_awarded = false;
   ```

4. **Логи начисления:**
   ```bash
   docker compose logs -f backend | grep -i "бонус\|loyalty\|bonus"
   ```

---

## ✅ Проверка работы

### 1. Проверка настроек

```bash
cd backend
python scripts/check_booking_sync.py
```

**Ожидаемый результат:**
```
✅ YCLIENTS_ENABLED: True
✅ YCLIENTS_COMPANY_ID: 235564
✅ YCLIENTS_API_TOKEN: Установлен
✅ YCLIENTS_USER_TOKEN: Установлен
✅ Пользователей с кодом: X из X
```

### 2. Проверка синхронизации услуг и мастеров

```bash
python scripts/sync_yclients_catalog.py
```

**Ожидаемый результат:**
```
INFO Услуги синхронизированы: создано X, обновлено Y
INFO Мастера синхронизированы: создано X, обновлено Y
INFO Синхронизация завершена
```

### 3. Проверка создания записи

1. Откройте приложение Flutter
2. Перейдите в меню → "Записаться онлайн"
3. Создайте тестовую запись в YClients
4. Проверьте логи бэкенда:
   ```bash
   docker compose logs -f backend | grep -i "webhook\|sync\|запись"
   ```
5. Проверьте запись в БД:
   ```bash
   python scripts/check_booking_sync.py
   ```
   Или через SQL:
   ```sql
   SELECT id, service_name, appointment_datetime, status, notes 
   FROM bookings 
   ORDER BY created_at DESC 
   LIMIT 5;
   ```

### 4. Проверка начисления бонусов

1. Отметьте запись как "Завершена" в YClients
2. Дождитесь синхронизации (30 минут) или запустите вручную:
   ```bash
   curl -X POST "http://localhost:9003/api/v1/yclients/sync-bookings" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```
3. Проверьте бонусы пользователя:
   ```sql
   SELECT id, email, loyalty_bonuses, spent_bonuses 
   FROM users 
   WHERE email = 'email@example.com';
   ```
4. Проверьте запись:
   ```sql
   SELECT id, status, loyalty_bonuses_awarded, loyalty_bonuses_amount 
   FROM bookings 
   WHERE status = 'completed' 
   ORDER BY updated_at DESC 
   LIMIT 5;
   ```

---

## 📝 Чеклист настройки

### Обязательные шаги

- [ ] Обновлен `.env` с токенами YClients
- [ ] Проверены права доступа User Token в YClients
- [ ] Запущен `check_yclients_config.py` - подключение успешно ✅
- [ ] Запущен `sync_yclients_catalog.py` - услуги и мастера синхронизированы ✅
- [ ] Все пользователи имеют `unique_code` (проверено через `check_booking_sync.py`) ✅
- [ ] Протестировано создание записи через форму
- [ ] Протестирована синхронизация записей
- [ ] Протестировано начисление бонусов

### Опциональные шаги

- [ ] Настроен webhook в YClients (для мгновенной синхронизации)
- [ ] Настроен ngrok для локальной разработки
- [ ] Протестировано списание бонусов по коду в админ панели

---

## 🚀 Готово к использованию!

После выполнения всех шагов система готова к работе:

1. ✅ Пользователи могут записываться через форму YClients
2. ✅ Записи автоматически синхронизируются с БД (webhook или каждые 30 минут)
3. ✅ Бонусы начисляются при завершении записи
4. ✅ Администраторы могут списывать бонусы по коду

## 📖 Дополнительные ресурсы

- **Быстрый старт:** `YCLIENTS_QUICK_START.md`
- **Настройка формы:** `YCLIENTS_FORM_SETUP.md`
- **Синхронизация записей:** `YCLIENTS_BOOKING_SAVE.md`
- **Синхронизация и бонусы:** `BOOKING_SYNC_EXPLAINED.md`

## ❓ Вопросы?

1. Проверьте раздел [Troubleshooting](#troubleshooting)
2. Проверьте логи бэкенда: `docker compose logs -f backend`
3. Запустите диагностику: `python scripts/check_booking_sync.py`

