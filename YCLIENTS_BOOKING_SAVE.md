# Как сохраняются записи из YClients

## Обзор

Все записи, созданные через форму YClients, **автоматически синхронизируются** с локальной базой данных. Это происходит двумя способами:

## 1. Webhook (мгновенная синхронизация)

### Как работает:
1. Пользователь создает запись через форму YClients
2. YClients отправляет webhook на наш сервер
3. Сервер получает данные записи и сохраняет в БД

### Настройка:
1. Перейдите в YClients: **Настройки → Интеграции → Webhooks**
2. Нажмите **"Добавить URL"**
3. Укажите URL: `https://ваш-домен.com/api/v1/yclients/webhook`
4. Выберите события:
   - ✅ Создание записи
   - ✅ Изменение записи
   - ✅ Отмена записи
5. Сохраните

### Преимущества:
- ⚡ Мгновенная синхронизация (запись появляется в БД сразу)
- 🔄 Автоматическое обновление при изменении статуса
- 📊 Всегда актуальные данные

## 2. Периодическая синхронизация (каждые 30 минут)

### Как работает:
1. Автоматическая задача запускается каждые 30 минут
2. Получает все записи из YClients за последние 30 дней
3. Синхронизирует их с локальной БД

### Настройка:
Автоматически включена, если `YCLIENTS_ENABLED=True` в `.env`

### Преимущества:
- 🔄 Резервный механизм (если webhook не сработал)
- 📅 Синхронизация старых записей
- 🛡️ Гарантия, что все записи будут в БД

## Как записи привязываются к пользователям

Система ищет пользователя в следующем порядке:

### 1. По уникальному коду (приоритет)
- Код добавляется в комментарий формы: `"Код клиента: ABC12345"`
- Самый надежный способ привязки

### 2. По email
- Точное совпадение email из YClients с email в нашей БД

### 3. По телефону
- Поиск по последним 10 цифрам номера телефона

## Что сохраняется в БД

Для каждой записи сохраняется:

- ✅ **ID записи из YClients** (в поле `notes`)
- ✅ **Уникальный код пользователя** (в поле `notes`)
- ✅ **Название услуги** (`service_name`)
- ✅ **Цена услуги** (`service_price` в копейках)
- ✅ **Длительность услуги** (`service_duration` в минутах)
- ✅ **Дата и время записи** (`appointment_datetime`)
- ✅ **Статус записи** (`status`: PENDING, CONFIRMED, COMPLETED, CANCELLED)
- ✅ **Телефон клиента** (`phone`)
- ✅ **Связь с пользователем** (`user_id`)

## Проверка синхронизации

### 1. Автоматическая проверка

Запустите скрипт проверки:

```bash
cd backend
python scripts/check_booking_sync.py
```

Скрипт покажет:
- Настройки YClients
- Количество записей в БД
- Пользователей с кодами
- Последние синхронизированные записи
- Рекомендации по настройке

### 2. Ручная синхронизация

Если нужно синхронизировать записи вручную:

```bash
curl -X POST "http://localhost:8000/api/v1/yclients/sync-bookings" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 3. Проверка записей пользователя

```bash
curl "http://localhost:8000/api/v1/booking?upcoming_only=true" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## Примеры

### Пример 1: Новая запись через форму

1. Пользователь открывает форму YClients
2. В комментарии автоматически добавляется: `"Код клиента: ABC12345"`
3. Пользователь заполняет форму и создает запись
4. **Webhook (мгновенно):**
   - YClients отправляет webhook
   - Система находит пользователя по коду `ABC12345`
   - Создается запись в БД со статусом `PENDING` или `CONFIRMED`
5. **Периодическая синхронизация (резерв):**
   - Если webhook не сработал, запись будет синхронизирована в течение 30 минут

### Пример 2: Изменение статуса записи

1. Администратор в YClients отмечает запись как завершенную
2. YClients отправляет webhook с обновленным статусом
3. Система обновляет запись в БД:
   - Статус меняется на `COMPLETED`
   - Автоматически начисляются бонусы (если включена программа лояльности)

### Пример 3: Отмена записи

1. Пользователь или администратор отменяет запись в YClients
2. YClients отправляет webhook
3. Система обновляет запись в БД:
   - Статус меняется на `CANCELLED`
   - Устанавливается `cancelled_at`

## Troubleshooting

### Записи не синхронизируются

1. **Проверьте настройки:**
   ```bash
   python scripts/check_booking_sync.py
   ```

2. **Проверьте логи бэкенда:**
   - Ищите сообщения с `"Синхронизация"` или `"webhook"`
   - Проверьте наличие ошибок

3. **Проверьте webhook:**
   - Убедитесь, что webhook настроен в YClients
   - Проверьте, что URL доступен из интернета
   - Проверьте логи webhook в YClients

4. **Проверьте уникальные коды:**
   - Убедитесь, что у пользователей есть `unique_code`
   - Запустите: `python scripts/generate_unique_codes.py`

### Пользователь не находится

1. **Проверьте код в комментарии:**
   - Убедитесь, что в форме YClients добавляется `"Код клиента: ..."`
   - Проверьте, что код совпадает с `unique_code` пользователя

2. **Проверьте email/телефон:**
   - Убедитесь, что данные в YClients совпадают с данными в нашей БД

3. **Проверьте логи:**
   - Ищите сообщения `"Пользователь не найден"` в логах

### Записи дублируются

1. **Проверьте поиск существующих записей:**
   - Система ищет записи по YClients ID в `notes`
   - Если ID не найден, создается новая запись

2. **Проверьте формат ID:**
   - ID должен быть в формате: `"YClients. ID: 12345"`

## Важные моменты

1. **Все записи сохраняются в локальную БД** - даже если они созданы в YClients
2. **Уникальный код** - самый надежный способ привязки записей
3. **Webhook + периодическая синхронизация** - двойная гарантия синхронизации
4. **Бонусы начисляются автоматически** при завершении записи (статус `COMPLETED`)

## Настройка для продакшена

1. ✅ Настройте webhook в YClients
2. ✅ Убедитесь, что `YCLIENTS_ENABLED=True`
3. ✅ Проверьте, что все пользователи имеют `unique_code`
4. ✅ Настройте мониторинг логов синхронизации
5. ✅ Регулярно проверяйте синхронизацию через скрипт

