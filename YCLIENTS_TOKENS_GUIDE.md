# 📋 Где взять токены YClients - подробная инструкция

## 🎯 Быстрый ответ

**Токены находятся в личном кабинете YClients:**
1. Войдите на https://yclients.com
2. Настройки → Интеграции → API
3. Скопируйте токены

**Если не нашли:**
- Обратитесь в поддержку: support@yclients.com или 8-800-500-33-83
- Скажите, что нужны API токены для интеграции

---

## 📸 Пошаговая инструкция с скриншотами (текстовое описание)

### Шаг 1: Вход в личный кабинет

1. Откройте https://yclients.com
2. Нажмите **"Войти"** в правом верхнем углу
3. Введите логин и пароль

### Шаг 2: Поиск раздела API

**Вариант A: Через меню (РЕКОМЕНДУЕТСЯ)**
1. В левом меню найдите **"Интеграции"** (между "Лояльность" и "Настройки")
2. Кликните на **"Интеграции"**
3. Внутри должен быть раздел **"API"** или **"API токены"**
4. Если нет - возможно нужен другой тариф или права администратора

**Вариант A2: Через Настройки**
1. В левом меню найдите **"Настройки"** (иконка шестерёнки)
2. В разделе "Системные настройки" может быть подраздел "API" или "Интеграции"

**Вариант B: Прямая ссылка**
- Попробуйте: `https://yclients.com/settings/integrations`
- Или: `https://yclients.com/settings/api`

**Вариант C: Через администрирование**
1. Меню → **"Администрирование"**
2. **"Настройки компании"** → **"Интеграции"**

### Шаг 3: Получение токенов

**Если раздел API найден:**

1. Найдите секцию **"API токены"** или **"Ключи API"**
2. Если токенов нет:
   - Нажмите **"Создать токен"** или **"Добавить ключ"**
   - Выберите права доступа (чтение/запись записей)
   - Сохраните
3. Скопируйте:
   - **API Token** (Partner Token) → это `YCLIENTS_API_TOKEN`
   - **User Token** (User Partner Token) → это `YCLIENTS_USER_TOKEN`

**Если раздела API нет:**

1. Обратитесь в поддержку YClients
2. Email: support@yclients.com
3. Телефон: 8-800-500-33-83
4. Скажите: *"Мне нужны API токены для интеграции моего приложения с YClients"*

### Шаг 4: Получение ID компании

**Способ 1: Из URL**
1. Откройте страницу расписания вашей компании
2. URL будет: `https://yclients.com/timetable/123456`
3. Число `123456` - это `YCLIENTS_COMPANY_ID`

**Способ 2: Из настроек**
1. Настройки → Информация о компании
2. Найдите поле **"ID компании"** или **"Company ID"**

**Способ 3: Через API (если есть токен)**
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
     https://api.yclients.com/api/v1/user
```
В ответе будет `company_id`

---

## 🔑 Что делать с токенами

### 1. Добавьте в `.env` файл

```env
# YClients Integration
YCLIENTS_ENABLED=True
YCLIENTS_COMPANY_ID=123456
YCLIENTS_API_TOKEN=ваш-api-token-здесь-длинная-строка
YCLIENTS_USER_TOKEN=ваш-user-token-здесь-длинная-строка
```

### 2. Перезапустите backend

```bash
cd backend
# Остановите сервер (Ctrl+C)
# Запустите снова
uvicorn app.main:app --reload
```

### 3. Проверьте работу

```bash
# Проверка через API (если есть тестовая услуга с yclients_service_id)
curl -H "Authorization: Bearer YOUR_USER_TOKEN" \
     http://localhost:8000/api/v1/yclients/widget-url/1
```

---

## ❓ Частые вопросы

### Q: Не могу найти раздел API в интерфейсе
**A:** Обратитесь в поддержку YClients. Не все тарифы включают доступ к API.

### Q: Что если у меня только один токен?
**A:** Попробуйте использовать один и тот же токен для обоих полей:
```env
YCLIENTS_API_TOKEN=один-токен
YCLIENTS_USER_TOKEN=один-токен
```

### Q: Токены не работают
**A:** Проверьте:
- Правильно ли скопировали (без пробелов)
- Есть ли права на создание записей
- Активны ли токены (не истекли)

### Q: Где найти ID услуги в YClients?
**A:**
1. Откройте услугу в YClients
2. Посмотрите в URL: `.../services/789`
3. Число `789` - это ID услуги
4. Введите его в поле `yclients_service_id` в админ-панели

### Q: Где найти ID мастера?
**A:**
1. Откройте мастера в YClients
2. Посмотрите в URL: `.../staff/456`
3. Число `456` - это ID мастера
4. Введите его в поле `yclients_staff_id` (опционально)

---

## 📞 Контакты поддержки YClients

- **Email:** support@yclients.com
- **Телефон:** 8-800-500-33-83 (бесплатно)
- **Онлайн-чат:** на сайте yclients.com
- **Время работы:** Пн-Пт 9:00-18:00 МСК

**Что сказать в поддержке:**
> "Здравствуйте! Мне нужны API токены для интеграции моего мобильного приложения с YClients. Нужны Partner Token и User Token для доступа к API. Также нужен ID моей компании."

---

## ✅ Чек-лист настройки

- [ ] Войдите в личный кабинет YClients
- [ ] Найдите раздел API (или обратитесь в поддержку)
- [ ] Скопируйте API Token
- [ ] Скопируйте User Token
- [ ] Найдите ID компании
- [ ] Добавили токены в `.env` файл
- [ ] Установили `YCLIENTS_ENABLED=True`
- [ ] Перезапустили backend
- [ ] Привязали услуги к YClients (указали `yclients_service_id`)
- [ ] Протестировали бронирование

---

## 🧪 Тестирование интеграции

### 1. Проверка настройки

```bash
# В логах backend должно быть:
# "YClients настроен и готов к работе"
```

### 2. Проверка виджета

1. Откройте услугу с привязанным `yclients_service_id`
2. Нажмите "Быстрое бронирование"
3. Должен открыться WebView с виджетом YClients
4. Если ошибка - проверьте токены и ID компании

### 3. Проверка API

```bash
# Получить URL виджета
curl -X GET \
  "http://localhost:8000/api/v1/yclients/widget-url/1" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"

# Должен вернуть:
# {
#   "widget_url": "https://yclients.com/timetable/123456?services=789",
#   "service_id": 1,
#   "service_name": "..."
# }
```

---

## 🔒 Безопасность

⚠️ **ВАЖНО:**
- Никогда не публикуйте токены в открытом доступе
- Не коммитьте `.env` файл в Git (добавьте в `.gitignore`)
- Используйте разные токены для разработки и продакшена
- Регулярно обновляйте токены (если есть такая возможность)
- Ограничьте права токенов только необходимыми операциями

---

## 📚 Дополнительные ресурсы

- **Документация YClients API:** https://api.yclients.com
- **Форум YClients:** https://yclients.com/forum
- **Поддержка:** support@yclients.com

