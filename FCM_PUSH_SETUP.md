# Настройка Push-уведомлений (FCM)

## Проблема
Уведомления не доходят до мобильного приложения.

## Причины и решения

### 1. FCM_SERVER_KEY не настроен на сервере

**Проверка:**
- Проверь логи бэкенда при отправке уведомления
- Если видишь `FCM_SERVER_KEY not configured, skipping push` - ключ не установлен

**Решение:**

1. Получи Server Key из Firebase Console:
   - Открой [Firebase Console](https://console.firebase.google.com/)
   - Выбери свой проект
   - Перейди в **Project Settings** (шестеренка рядом с названием проекта)
   - Открой вкладку **Cloud Messaging**
   - Скопируй **Server Key** (или **Legacy server key**)

2. Добавь в `.env` на сервере:
   ```bash
   FCM_SERVER_KEY=your-server-key-here
   ```

3. Перезапусти бэкенд:
   ```bash
   docker-compose restart backend
   ```

### 2. Токены устройств не регистрируются

**Проверка:**
- Запусти приложение и залогинься
- Проверь логи приложения (должны быть сообщения о регистрации токена)
- Проверь в базе данных таблицу `device_tokens`:
   ```sql
   SELECT * FROM device_tokens WHERE is_active = true;
   ```

**Решение:**
- Убедись, что пользователь залогинен (токены регистрируются только для авторизованных пользователей)
- Проверь, что приложение имеет разрешения на уведомления
- Проверь логи приложения на наличие ошибок

### 3. Неправильная платформа устройства

**Исправлено в коде:**
- Теперь платформа определяется автоматически (Android/iOS)
- Добавлено логирование для отладки

### 4. Уведомления не отображаются в форграунде

**Текущее состояние:**
- Уведомления в форграунде только логируются в консоль
- Для отображения нужно установить `flutter_local_notifications`

**Решение (опционально):**
Если нужно показывать уведомления в форграунде:
```bash
flutter pub add flutter_local_notifications
```

### 5. Проверка работы уведомлений

**Шаги для тестирования:**

1. **Проверь регистрацию токена:**
   - Открой приложение
   - Залогинься
   - Проверь логи: должны быть сообщения `🔑 FCM token obtained` и `✅ Device token registered`

2. **Проверь наличие токенов в БД:**
   ```sql
   SELECT COUNT(*) FROM device_tokens WHERE is_active = true;
   ```

3. **Отправь тестовое уведомление:**
   - В админ-панели создай новую кампанию
   - Установи статус `SENT`
   - Проверь логи бэкенда на наличие ошибок

4. **Проверь логи бэкенда:**
   - Должны быть сообщения о количестве успешных/неуспешных отправок
   - Если все неуспешные - проверь FCM_SERVER_KEY

### 6. Типичные ошибки FCM

**401 Unauthorized:**
- Неверный FCM_SERVER_KEY
- Проверь, что ключ скопирован полностью, без пробелов

**InvalidRegistration:**
- Токен устройства недействителен
- Устройство нужно перерегистрировать (переустановить приложение или очистить данные)

**NotRegistered:**
- Токен был удален из FCM (например, приложение было удалено)
- Токен нужно удалить из БД

**MismatchSenderId:**
- FCM_SERVER_KEY не соответствует проекту Firebase
- Проверь, что используешь правильный проект

## Логи для отладки

**В приложении (Flutter):**
- `🔑 FCM token obtained` - токен получен
- `✅ Device token registered` - токен зарегистрирован на сервере
- `📬 Push notification received` - уведомление получено
- `❌ Error...` - ошибки

**На бэкенде:**
- `FCM_SERVER_KEY not configured` - ключ не установлен
- `Starting push campaign` - начало отправки
- `FCM chunk processed` - результаты отправки
- `FCM delivery failed` - ошибки доставки

## Быстрая проверка

1. Проверь `.env` на сервере - есть ли `FCM_SERVER_KEY`
2. Проверь логи приложения - регистрируется ли токен
3. Проверь БД - есть ли активные токены
4. Отправь тестовое уведомление через админ-панель
5. Проверь логи бэкенда на ошибки

