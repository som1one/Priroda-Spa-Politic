# Текущий процесс бронирования - подробное описание

## 📋 Текущее состояние системы

### ✅ Что ЕСТЬ сейчас:

1. **Выбор услуги** - из меню категорий
2. **Цена услуги** - фиксированная, берётся из `service.price` в БД
3. **Длительность** - фиксированная, берётся из `service.duration` в БД
4. **Дополнительные услуги** - можно выбрать несколько, но **без цен** (TODO)
5. **Выбор даты и времени** - через календарь
6. **Контактные данные** - телефон (автозаполнение из профиля)
7. **Комментарий** - необязательное поле
8. **Баллы лояльности** - можно использовать при оплате

### ❌ Чего НЕТ сейчас (в нашем календаре):

1. **Выбор мастера** - не реализован в нашем календаре
2. **Цены для дополнительных услуг** - не учитываются в итоговой стоимости
3. **Динамические цены** - цена фиксированная, не зависит от мастера/времени
4. **Проверка доступности времени** - статичный список слотов (09:00-20:00)

### ✅ Что ЕСТЬ в YClients виджете:

1. **Выбор мастера** - ✅ ЕСТЬ! Виджет автоматически показывает выбор
2. **Реальные доступные слоты** - ✅ ЕСТЬ! Из расписания YClients
3. **Динамические цены** - ✅ ЕСТЬ! Могут зависеть от мастера
4. **Дополнительные услуги с ценами** - ✅ ЕСТЬ! Если настроено в YClients

---

## 🔄 Процесс бронирования (пошагово)

### Шаг 1: Выбор услуги
**Экран:** `MenuSpaScreen` → `ServiceDetailScreen`

**Что происходит:**
- Пользователь видит категории услуг
- Выбирает услугу
- Видит детали: название, описание, **фиксированную цену**, длительность
- Нажимает "Быстрое бронирование"

**Данные из БД:**
```json
{
  "id": 1,
  "name": "Расслабляющий аромамассаж",
  "price": 2500.00,  // ← Фиксированная цена
  "duration": 60,    // ← Фиксированная длительность
  "yclients_service_id": null,  // ← Если null - наш календарь, если есть - YClients
  "yclients_staff_id": null     // ← ID мастера в YClients (опционально)
}
```

---

### Шаг 2: Выбор даты и времени

#### Вариант A: Наш календарь (если услуга НЕ привязана к YClients)
**Экран:** `BookingScreen`

**Что происходит:**
- Показывается календарь (90 дней вперёд)
- Пользователь выбирает дату
- Показывается список времен: **09:00, 10:00, 11:00... 20:00**
- Пользователь выбирает время
- Нажимает "Подтвердить бронирование"

**Ограничения:**
- ❌ **НЕТ выбора мастера** - мастер не выбирается
- ❌ **НЕТ проверки реальной доступности** - показываются все времена
- ✅ Для сегодняшней даты фильтруются прошедшие времена
- ✅ Минимальное время: текущее + 1 час

#### Вариант B: YClients виджет (если услуга привязана к YClients)
**Экран:** `YClientsBookingScreen` (WebView)

**Что происходит:**
- Открывается веб-виджет YClients в WebView
- Пользователь видит полный процесс бронирования:
  - ✅ **Выбор мастера** (если несколько мастеров для услуги)
  - ✅ **Выбор даты** (календарь с доступными датами)
  - ✅ **Выбор времени** (реальные доступные слоты из расписания)
  - ✅ **Цены** (могут отличаться у разных мастеров)
  - ✅ **Дополнительные услуги** (если настроены в YClients)
- После бронирования возврат в наше приложение

**Важно:**
- Выбор мастера работает автоматически, если в YClients настроено несколько мастеров
- Если указан `yclients_staff_id` - мастер фиксированный (выбора нет)
- Если `yclients_staff_id = NULL` - виджет покажет выбор мастера

---

### Шаг 3: Детали бронирования
**Экран:** `BookingDetailsScreen`

**Что происходит:**
- Показывается выбранная дата и время
- Можно выбрать **дополнительные услуги** (чекбоксы)
  - ⚠️ **НО цены не учитываются в итоговой стоимости!**
- Заполняется телефон (автозаполнение из профиля)
- Можно добавить комментарий
- Показывается **итоговая стоимость** = базовая цена услуги
- Нажимает "Продолжить к оплате"

**Текущая логика расчёта цены:**
```dart
double _calculateTotalPrice() {
  double total = _service?.price ?? 0.0;
  // TODO: Добавить цены для дополнительных услуг
  return total;
}
```

---

### Шаг 4: Оплата
**Экран:** `PayScreen`

**Что происходит:**
- Заполнение данных карты
- Можно использовать баллы лояльности (если есть)
- Нажимает "Оплатить"
- Создаётся запись в БД через `POST /api/v1/bookings`

**Данные, которые отправляются:**
```json
{
  "service_name": "Расслабляющий аромамассаж",
  "service_duration": 60,
  "service_price": 250000,  // ← В копейках (2500.00 * 100)
  "appointment_datetime": "2024-11-20T10:00:00Z",
  "phone": "+79991234567",
  "notes": "Комментарий + дополнительные услуги"
}
```

**Важно:**
- ❌ **НЕТ информации о мастере** - не передаётся
- ❌ **НЕТ реальной проверки доступности** - создаётся запись с любым временем
- ✅ Цена фиксированная из услуги
- ✅ Дополнительные услуги записываются в `notes`, но не влияют на цену

---

### Шаг 5: Подтверждение
**Экран:** Диалог подтверждения

**Что происходит:**
- Показывается успешное сообщение
- Отправляется email подтверждения (background task)
- Возврат на главный экран

---

## 🎯 Итоговая схема данных

### Что сохраняется в БД:
```sql
bookings:
  - id
  - user_id
  - service_name          -- Только название, не ID услуги!
  - service_duration      -- В минутах
  - service_price         -- В копейках
  - appointment_datetime   -- Дата и время
  - status                -- "pending" по умолчанию
  - phone                 -- Телефон клиента
  - notes                 -- Комментарий + дополнительные услуги (текстом)
  - created_at
  - updated_at
```

### Чего НЕТ в записи:
- ❌ `service_id` - только `service_name` (строка)
- ❌ `master_id` / `staff_id` - нет выбора мастера
- ❌ `additional_services` (структурированно) - только в `notes`
- ❌ Цены дополнительных услуг

---

## 🔧 Что нужно добавить для полной функциональности

### 1. Выбор мастера
**Где добавить:**
- В `BookingDetailsScreen` или отдельный шаг
- Получать список мастеров через API
- Сохранять `master_id` в бронировании

**API endpoint (нужно создать):**
```
GET /api/v1/masters?service_id={service_id}
```

### 2. Цены для дополнительных услуг
**Где добавить:**
- В модель `Service.additional_services` добавить поле `price`
- Обновить `_calculateTotalPrice()` в `BookingDetailsScreen`
- Учитывать в итоговой стоимости

### 3. Проверка доступности времени
**Где добавить:**
- Получать реальные доступные слоты через YClients API (если привязано)
- Или создать свой endpoint для проверки занятости
- Показывать только свободные времена

### 4. Динамические цены
**Где добавить:**
- Цена может зависеть от мастера
- Цена может зависеть от времени (дневная/вечерняя)
- Сохранять в `service_price` актуальную цену

---

## 📱 Интеграция с YClients

### Если услуга привязана к YClients:

**Процесс:**
1. Выбор услуги → проверка `yclients_service_id`
2. Если есть → открывается `YClientsBookingScreen` (WebView)
3. Загружается URL: `https://yclients.com/timetable/{company_id}?services={service_id}`
4. Пользователь бронирует через виджет YClients
5. В виджете автоматически есть:
   - ✅ **Выбор мастера** (если `yclients_staff_id = NULL` и несколько мастеров)
   - ✅ Реальные доступные слоты (из расписания YClients)
   - ✅ Правильные цены (могут зависеть от мастера)
   - ✅ Дополнительные услуги (если настроены)
   - ✅ Всё что нужно для полноценного бронирования

**Преимущества:**
- ✅ Не нужно реализовывать выбор мастера - он уже есть в виджете
- ✅ Автоматическая синхронизация с расписанием
- ✅ Реальные доступные времена
- ✅ Динамические цены
- ✅ Управление мастерами через YClients

**YPlaces:**
- Это отдельное мобильное приложение от YClients
- Мы его НЕ используем
- Мы используем веб-виджет YClients через WebView
- В веб-виджете тоже есть выбор мастера

---

## 🔑 Где взять токены для YClients

### Пошаговая инструкция:

#### 1. Войдите в личный кабинет YClients
- Откройте https://yclients.com
- Войдите в свой аккаунт

#### 2. Перейдите в настройки API
- В меню выберите **"Настройки"** (или **"Администрирование"**)
- Найдите раздел **"API"** или **"Интеграции"**
- Или прямой путь: **"Настройки → API"**

#### 3. Создайте API токен
- Нажмите **"Создать токен"** или **"Добавить API ключ"**
- Скопируйте **API Token** (длинная строка)
- Это будет `YCLIENTS_API_TOKEN`

#### 4. Получите User Token
- В том же разделе найдите **"User Token"** или **"Токен пользователя"**
- Если его нет - создайте или используйте основной токен
- Это будет `YCLIENTS_USER_TOKEN`

#### 5. Найдите ID компании
- В URL страницы вашей компании будет число: `https://yclients.com/timetable/123456`
- Или в настройках компании → "Информация о компании"
- Число `123456` - это `YCLIENTS_COMPANY_ID`

#### 6. Добавьте в .env
```env
YCLIENTS_ENABLED=True
YCLIENTS_COMPANY_ID=123456
YCLIENTS_API_TOKEN=ваш-api-token-здесь
YCLIENTS_USER_TOKEN=ваш-user-token-здесь
```

### Альтернативный способ (если не нашли в интерфейсе):

1. **Обратитесь в поддержку YClients:**
   - Email: support@yclients.com
   - Телефон: 8-800-500-33-83
   - Скажите, что нужны API ключи для интеграции

2. **Или через партнёрский портал:**
   - https://api.yclients.com
   - Там может быть раздел для получения токенов

### Важно:
- ⚠️ Токены дают доступ к вашим данным - храните их в секрете
- ⚠️ Не коммитьте `.env` файл в Git
- ⚠️ Используйте разные токены для разработки и продакшена

---

## 📊 Сравнение: Наш календарь vs YClients

| Функция | Наш календарь | YClients виджет |
|---------|---------------|----------------|
| Выбор даты | ✅ | ✅ |
| Выбор времени | ✅ (статичный список) | ✅ (реальные слоты) |
| Выбор мастера | ❌ | ✅ |
| Проверка доступности | ❌ | ✅ |
| Цены | ✅ (фиксированные) | ✅ (динамические) |
| Дополнительные услуги | ✅ (без цен) | ✅ (с ценами) |
| Синхронизация | ❌ | ✅ |

---

## 🚀 Рекомендации

### Для быстрого запуска:
1. Используйте YClients для всех услуг - просто привяжите их
2. Не нужно реализовывать выбор мастера - это уже есть в YClients
3. Реальные доступные слоты работают автоматически

### Для кастомного решения:
1. Добавьте выбор мастера в `BookingDetailsScreen`
2. Создайте API для получения мастеров
3. Добавьте цены для дополнительных услуг
4. Реализуйте проверку доступности времени

---

## 📝 Итого

**Текущее состояние:**
- ✅ Базовое бронирование работает
- ✅ Цена фиксированная из услуги
- ❌ Выбор мастера не реализован
- ❌ Дополнительные услуги без цен
- ✅ YClients интеграция готова (нужно только настроить)

**Для полной функциональности:**
- Либо используйте YClients (проще)
- Либо доработайте наш календарь (больше работы)

