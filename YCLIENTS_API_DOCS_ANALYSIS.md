# 📚 Анализ документации YClients API

## 🔍 Что есть в документации:

### ✅ В документации описано:

1. **Как использовать токены:**
   - В заголовках запросов: `Authorization: Bearer partner_token, User user_token`
   - Это подтверждает, что нужны **два токена**:
     - `partner_token` (Partner Token / API Token)
     - `user_token` (User Token) ✅ у вас уже есть

2. **API методы:**
   - Авторизация пользователя
   - Онлайн-запись
   - Записи
   - Услуги
   - И т.д.

3. **Формат запросов:**
   - Как делать API запросы
   - Какие параметры нужны
   - Формат ответов

---

## ❌ Чего НЕТ в документации:

1. **Где получить токены:**
   - Документация НЕ объясняет, где найти Partner Token и User Token
   - Это нужно делать через **веб-интерфейс YClients**

2. **Где найти Company ID:**
   - Документация НЕ объясняет, где взять Company ID
   - Это нужно искать в **URL расписания** или **настройках компании**

---

## 🎯 Что мы ищем:

### 1. Partner Token (API Token)
   - ❌ В документации API его нет
   - ✅ Нужно искать в **веб-интерфейсе YClients**:
     - Раздел "Интеграции" → "Другое" → "API"
     - Или портал: https://api.yclients.com

### 2. Company ID
   - ❌ В документации API его нет
   - ✅ Нужно искать в **URL расписания**:
     - `https://yclients.com/timetable/123456`
     - Число `123456` - это Company ID

### 3. User Token
   - ✅ У вас уже есть: `8078d2cc6eba4853178aac2ea7793299`

---

## 📋 Что подтверждает документация:

### Формат авторизации:

```
Authorization: Bearer partner_token, User user_token
```

Это означает:
- **Partner Token** - это `partner_token` в заголовке
- **User Token** - это `user_token` в заголовке
- Оба токена нужны для работы с API

---

## 🔧 Где искать недостающие данные:

### Partner Token (API Token):

**Вариант 1: В веб-интерфейсе YClients**
1. Раздел **"Интеграции"** → категория **"Другое"**
2. Там должен быть раздел **"API"** или **"Партнёрский доступ"**
3. Откройте его - там будут токены

**Вариант 2: Партнёрский портал**
1. Откройте: https://api.yclients.com
2. Войдите в свой аккаунт
3. Найдите ваше приложение
4. Там будут оба токена

**Вариант 3: Поддержка YClients**
- Email: support@yclients.com
- Телефон: 8-800-500-33-83

---

### Company ID:

**Способ 1: Из URL расписания**
1. Откройте страницу расписания вашей компании в YClients
2. URL будет: `https://yclients.com/timetable/123456`
3. Число `123456` - это ваш Company ID

**Способ 2: Из настроек**
1. Зайдите в **"Настройки"** → **"О компании"**
2. Там должен быть **"ID компании"**

---

## ✅ Итог:

### В документации API есть:
- ✅ Описание, как использовать токены
- ✅ API методы и форматы запросов
- ✅ Примеры использования

### В документации API НЕТ:
- ❌ Где получить Partner Token
- ❌ Где получить Company ID
- ❌ Где получить User Token (но у вас уже есть)

### Что делать:
1. **Partner Token** - искать в веб-интерфейсе YClients (Интеграции → API)
2. **Company ID** - искать в URL расписания
3. **User Token** - уже есть ✅

---

## 📝 Вывод:

**Документация API описывает, как использовать токены, но не где их получить.**

Токены нужно получать через **веб-интерфейс YClients** или **партнёрский портал**.

Документация API полезна для понимания, как делать запросы, но для получения токенов нужно искать в другом месте! 🔍

