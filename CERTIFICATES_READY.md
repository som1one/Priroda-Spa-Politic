# ✅ Сертификаты готовы к использованию!

## 📋 Что создано:

### ✅ Файлы сертификатов:
1. **`certificate.p12`** - Сертификат для подписи (3.2 KB)
2. **`Priroda_Spa.mobileprovision`** - Provisioning Profile (12 KB)
3. **`certificate_base64.txt`** - Сертификат в base64 для GitHub
4. **`profile_base64.txt`** - Provisioning Profile в base64 для GitHub

### ✅ Исходные файлы:
- `ios_distribution.cer` - исходный сертификат от Apple
- `private_key.pem` - приватный ключ (храните в безопасности!)

---

## 🔑 Следующий шаг: Добавьте секреты в GitHub

### 1. Откройте GitHub Secrets

1. GitHub → ваш репозиторий
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**

### 2. Добавьте 4 секрета:

#### Секрет 1: `APPLE_CERTIFICATE_BASE64`
- **Name:** `APPLE_CERTIFICATE_BASE64`
- **Secret:** Откройте `certificate_base64.txt` и скопируйте **весь текст**
- **Add secret**

#### Секрет 2: `APPLE_CERTIFICATE_PASSWORD`
- **Name:** `APPLE_CERTIFICATE_PASSWORD`
- **Secret:** Пароль, который вы использовали при создании `.p12`
- **Add secret**

#### Секрет 3: `APPLE_PROVISIONING_PROFILE_BASE64`
- **Name:** `APPLE_PROVISIONING_PROFILE_BASE64`
- **Secret:** Откройте `profile_base64.txt` и скопируйте **весь текст**
- **Add secret**

#### Секрет 4: `KEYCHAIN_PASSWORD` (опционально)
- **Name:** `KEYCHAIN_PASSWORD`
- **Secret:** Любой пароль (например, `temp_password`)
- **Add secret**

---

## 🚀 После добавления секретов:

### Запустите сборку:

1. GitHub → **Actions**
2. Выберите **"iOS Build"** workflow
3. **Run workflow** → выберите ветку (`main`) → **Run workflow**

### Скачайте IPA:

После успешной сборки:
1. Откройте завершенный workflow run
2. Прокрутите вниз до **"Artifacts"**
3. Нажмите **"ios-ipa"**
4. Скачайте `.ipa` файл

---

## ⚠️ Важно:

1. **Не коммитьте** файлы сертификатов в Git (они в `.gitignore`)
2. **Храните** пароль от `.p12` в безопасном месте
3. **Не удаляйте** `certificate.p12` и `Priroda_Spa.mobileprovision` - могут понадобиться

---

## 📝 Быстрая команда для открытия base64 файлов:

```powershell
# Откройте certificate_base64.txt
notepad certificate_base64.txt

# Откройте profile_base64.txt
notepad profile_base64.txt
```

**Скопируйте весь текст** из каждого файла (включая все символы, без пробелов в начале/конце).

---

**Готово!** Теперь добавьте секреты в GitHub и запустите сборку! 🎉

