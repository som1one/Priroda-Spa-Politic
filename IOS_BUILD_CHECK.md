# Проверка iOS билда и возможные проблемы

## ✅ Что было исправлено

### 1. Info.plist - добавлены необходимые разрешения
- ✅ `LSApplicationQueriesSchemes` - для работы `url_launcher` (открытие внешних ссылок)
- ✅ `NSAppTransportSecurity` - настройки безопасности сети
- ✅ `ITSAppUsesNonExemptEncryption` - для публикации в App Store

### 2. Runner.entitlements - создан файл для push-уведомлений
- ✅ `aps-environment` - для работы Firebase Cloud Messaging на iOS

### 3. Проверено наличие файлов
- ✅ `GoogleService-Info.plist` - присутствует (нужен для Firebase)
- ✅ `Info.plist` - обновлен с необходимыми разрешениями

## ⚠️ Важные моменты для iOS

### 1. Ошибки при билде

**Flutter покажет ошибки в следующих случаях:**

1. **Ошибки компиляции:**
   - Синтаксические ошибки в Dart коде
   - Отсутствующие зависимости в `pubspec.yaml`
   - Проблемы с импортами

2. **Ошибки конфигурации iOS:**
   - Отсутствующий `GoogleService-Info.plist`
   - Неправильный Bundle ID
   - Проблемы с подписями (Signing & Capabilities)
   - Отсутствующие разрешения в Info.plist

3. **Ошибки времени выполнения:**
   - Ошибки будут видны в консоли Xcode
   - Можно использовать `flutter logs` для просмотра

**Команды для проверки:**
```bash
# Проверка конфигурации
flutter doctor -v

# Анализ кода
flutter analyze

# Проверка зависимостей
flutter pub get
flutter pub outdated

# Билд для проверки ошибок (без установки)
flutter build ios --debug --no-codesign
```

### 2. Элементы, которые могут не работать на iOS

#### ✅ Должны работать:
- **WebView** (`webview_flutter`) - используется для VK авторизации
  - ✅ Настроен `JavaScriptMode.unrestricted`
  - ✅ Обработка навигации через `NavigationDelegate`
  
- **url_launcher** - открытие внешних ссылок
  - ✅ Добавлены схемы в `LSApplicationQueriesSchemes`
  - ✅ Используется `LaunchMode.externalApplication`

- **Firebase Messaging** - push-уведомления
  - ✅ Настроен запрос разрешений через `requestPermission()`
  - ✅ Создан `Runner.entitlements` с `aps-environment`

- **Google Sign-In** - авторизация через Google
  - ✅ Настроен `serverClientId` в `auth_service.dart`
  - ✅ Настроен `iosClientId` в `firebase_options.dart`

#### ⚠️ Потенциальные проблемы:

1. **WebView на iOS:**
   - Может требовать дополнительных разрешений для некоторых доменов
   - Проверьте, что VK OAuth URL доступен
   - На iOS 14+ может потребоваться `WKAppBoundDomains` для ограниченных доменов

2. **url_launcher:**
   - На iOS может не работать для некоторых схем без `LSApplicationQueriesSchemes` (уже добавлено)
   - Проверьте, что все используемые схемы добавлены в список

3. **Push-уведомления:**
   - Требуют правильной настройки в Apple Developer Portal
   - Нужен APNs сертификат или ключ
   - Для production нужно изменить `aps-environment` на `production` в entitlements

4. **Google Sign-In:**
   - Требует правильной настройки OAuth клиента в Google Console
   - `iosClientId` должен соответствовать iOS клиенту в Google Console

### 3. Что нужно проверить перед публикацией

#### В Xcode:
1. **Signing & Capabilities:**
   - ✅ Push Notifications capability добавлена
   - ✅ Правильный Team выбран
   - ✅ Bundle Identifier соответствует настройкам в Apple Developer

2. **Info.plist:**
   - ✅ Все разрешения добавлены
   - ✅ `ITSAppUsesNonExemptEncryption` установлен в `false` (если не используете шифрование)

3. **GoogleService-Info.plist:**
   - ✅ Файл добавлен в проект
   - ✅ Правильный Bundle ID в файле

#### В Apple Developer Portal:
1. **App ID:**
   - ✅ Push Notifications включены
   - ✅ Bundle ID соответствует проекту

2. **Certificates:**
   - ✅ APNs сертификат или ключ создан
   - ✅ Сертификат разработки/распространения создан

3. **Provisioning Profiles:**
   - ✅ Профиль создан и скачан
   - ✅ Профиль установлен в Xcode

### 4. Тестирование на реальном устройстве

**Важно:** Некоторые функции работают только на реальном устройстве:

- Push-уведомления (не работают в симуляторе)
- Google Sign-In (может работать некорректно в симуляторе)
- Некоторые системные диалоги

**Команды для тестирования:**
```bash
# Установка на подключенное устройство
flutter run -d <device-id>

# Просмотр логов
flutter logs

# Билд для тестирования
flutter build ios --debug
```

### 5. Частые ошибки и решения

#### Ошибка: "Missing GoogleService-Info.plist"
**Решение:** Убедитесь, что файл добавлен в Xcode проект и находится в папке `Runner`

#### Ошибка: "Push notifications not working"
**Решение:** 
- Проверьте `Runner.entitlements` - должен быть `aps-environment`
- Проверьте настройки в Apple Developer Portal
- Для production измените `aps-environment` на `production`

#### Ошибка: "url_launcher не открывает ссылки"
**Решение:** 
- Проверьте, что схема добавлена в `LSApplicationQueriesSchemes` в Info.plist
- Для iOS 9+ все внешние схемы должны быть объявлены

#### Ошибка: "WebView не загружает страницу"
**Решение:**
- Проверьте `NSAppTransportSecurity` в Info.plist
- Убедитесь, что домен разрешен для загрузки
- Проверьте, что JavaScript включен

### 6. Проверка перед финальным билдом

```bash
# 1. Очистка проекта
flutter clean

# 2. Получение зависимостей
flutter pub get

# 3. Анализ кода
flutter analyze

# 4. Проверка конфигурации
flutter doctor -v

# 5. Тестовый билд
flutter build ios --release --no-codesign

# 6. Если все ОК - билд через Xcode или Codemagic
```

### 7. Дополнительные рекомендации

1. **Тестирование на разных версиях iOS:**
   - Минимальная версия указана в `ios/Podfile` (обычно iOS 12.0+)
   - Тестируйте на iOS 15, 16, 17

2. **Производительность:**
   - Используйте Release билд для тестирования производительности
   - Проверьте использование памяти через Instruments

3. **Безопасность:**
   - Не храните секреты в коде
   - Используйте переменные окружения для API ключей
   - Проверьте настройки `NSAppTransportSecurity`

## 📝 Чеклист перед публикацией

- [ ] `flutter doctor -v` показывает все зеленые галочки
- [ ] `flutter analyze` не показывает ошибок
- [ ] `GoogleService-Info.plist` присутствует и правильный
- [ ] `Info.plist` содержит все необходимые разрешения
- [ ] `Runner.entitlements` настроен для push-уведомлений
- [ ] Bundle ID соответствует Apple Developer Portal
- [ ] Push Notifications capability добавлена в Xcode
- [ ] Протестировано на реальном iOS устройстве
- [ ] Push-уведомления работают
- [ ] Google Sign-In работает
- [ ] WebView загружает страницы
- [ ] url_launcher открывает ссылки
- [ ] Для production изменен `aps-environment` на `production`

## 🔍 Где смотреть ошибки

1. **При билде через Flutter:**
   - Консоль, где запускается команда `flutter build ios`
   - Файлы логов в `build/ios/`

2. **При билде через Xcode:**
   - Панель Issues (⌘+4)
   - Консоль (⌘+Shift+Y)
   - Report Navigator (⌘+9)

3. **Во время выполнения:**
   - Xcode консоль
   - `flutter logs`
   - Системные логи iOS через Console.app

4. **При публикации в App Store:**
   - App Store Connect - раздел "TestFlight" или "App Review"
   - Email от Apple с деталями отклонения

## 📞 Полезные ссылки

- [Flutter iOS Deployment](https://docs.flutter.dev/deployment/ios)
- [Firebase iOS Setup](https://firebase.google.com/docs/ios/setup)
- [url_launcher iOS Setup](https://pub.dev/packages/url_launcher#ios)
- [webview_flutter iOS Setup](https://pub.dev/packages/webview_flutter#ios)
- [Apple Developer Documentation](https://developer.apple.com/documentation/)

