# 🔑 Зачем нужен Partner Token (API Token)?

## 🎯 Partner Token - это обязательный токен для работы с YClients API!

---

## 📋 Что делает Partner Token:

### 1. **Авторизация в API**

Partner Token используется для **авторизации всех запросов** к YClients API.

**Формат авторизации (из документации YClients):**
```
Authorization: Bearer partner_token, User user_token
```

**Это означает:**
- `partner_token` (Partner Token) - идентифицирует **ваше приложение**
- `user_token` (User Token) - идентифицирует **вашего пользователя**

**Оба токена обязательны!** Без Partner Token API запросы не будут работать.

---

## 🔒 Зачем два токена:

### Partner Token (API Token):
- ✅ Идентифицирует **ваше приложение** в YClients
- ✅ Даёт доступ к API от имени приложения
- ✅ Проверяет, что запрос идёт от авторизованного приложения

### User Token:
- ✅ Идентифицирует **конкретного пользователя** (вас)
- ✅ Определяет права доступа к данным компании
- ✅ Связывает запросы с вашей учётной записью

**Вместе они обеспечивают безопасную авторизацию!**

---

## 💻 Где используется в коде:

### В нашем коде (`yclients_service.py`):

```python
def _get_headers(self) -> Dict[str, str]:
    """Получить заголовки для запросов"""
    headers = {
        "Authorization": f"Bearer {self.api_token}, User {self.user_token}",
        # ...
    }
    return headers
```

**Каждый запрос к YClients API использует оба токена!**

---

## ❌ Что будет без Partner Token:

### Без Partner Token:
- ❌ API запросы вернут ошибку **401 Unauthorized**
- ❌ Нельзя получить доступные даты для записи
- ❌ Нельзя создать запись через API
- ❌ Нельзя получить виджет URL
- ❌ **Интеграция не будет работать!**

### С Partner Token:
- ✅ Все API запросы работают
- ✅ Можно получать расписание
- ✅ Можно создавать записи
- ✅ Можно использовать виджет YClients
- ✅ **Интеграция работает!**

---

## 📝 Пример использования:

### Запрос к YClients API:

```python
headers = {
    "Authorization": "Bearer PARTNER_TOKEN, User USER_TOKEN",
    "Accept": "application/json",
    "Content-Type": "application/json",
}

response = await client.get(
    "https://api.yclients.com/api/v1/book_dates/123456",
    headers=headers
)
```

**Без Partner Token этот запрос вернёт 401!**

---

## 🎯 Итог:

### Partner Token нужен для:
1. ✅ **Авторизации** всех запросов к YClients API
2. ✅ **Идентификации** вашего приложения
3. ✅ **Безопасности** - проверка, что запрос от авторизованного приложения
4. ✅ **Работы интеграции** - без него ничего не работает!

### Без Partner Token:
- ❌ API не работает
- ❌ Интеграция не работает
- ❌ Запросы возвращают 401 Unauthorized

---

## ✅ Вывод:

**Partner Token - это обязательный токен для работы с YClients API!**

Без него интеграция **не будет работать**. Это как пароль для вашего приложения в YClients.

**Нужно обязательно найти Partner Token!** 🔑

