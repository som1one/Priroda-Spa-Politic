# 🚀 Гайд по развертыванию и проверке приложения

Полная инструкция по настройке и запуску всех компонентов приложения SPA салона.

## ⚡ Быстрый старт

Если вы хотите быстро запустить все компоненты:

1. **База данных (Docker):**
   ```bash
   cd backend
   docker-compose up -d postgres redis
   ```

2. **Backend:**
   ```bash
   cd backend
   python -m venv venv
   venv\Scripts\activate  # Windows
   # source venv/bin/activate  # Linux/Mac
   pip install -r requirements.txt
   copy env.template .env  # Windows
   # cp env.template .env  # Linux/Mac
   # Отредактируйте .env файл
   alembic upgrade head
   uvicorn app.main:app --reload
   ```

3. **Admin панель:**
   ```bash
   cd admin
   npm install
   npm run dev
   ```

4. **Flutter приложение:**
   ```bash
   cd spa
   flutter pub get
   flutter run --dart-define=SERVER_BASE_URL=http://10.0.2.2:8000  # Android
   # или
   flutter run --dart-define=SERVER_BASE_URL=http://localhost:8000  # iOS
   ```

**Подробные инструкции см. ниже ↓**

---

## 📋 Содержание

1. [Требования](#требования)
2. [База данных (PostgreSQL)](#база-данных-postgresql)
3. [Backend (FastAPI)](#backend-fastapi)
4. [Flutter приложение](#flutter-приложение)
5. [Admin панель](#admin-панель)
6. [Проверка работоспособности](#проверка-работоспособности)
7. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

### Необходимое ПО:

- **Python 3.11+** - для backend
- **PostgreSQL 14+** - база данных
- **Node.js 18+** и **npm/yarn** - для admin панели
- **Flutter 3.9+** - для мобильного приложения
- **Git** - система контроля версий

### Дополнительно (опционально):

- **Docker** и **Docker Compose** - для упрощения развертывания БД
- **Redis** - для кэширования (если включено)

---

## 🗄️ База данных (PostgreSQL)

### Вариант 1: Docker (рекомендуется)

1. Перейдите в директорию `backend`:

```bash
cd backend
```

2. Запустите PostgreSQL (файл `docker-compose.yml` уже существует):

```bash
docker-compose up -d
```

3. Проверьте, что контейнер запущен:

```bash
docker ps
```

Вы должны увидеть контейнер `spa_postgres`.

### Вариант 2: Локальная установка

1. Установите PostgreSQL с официального сайта
2. Создайте базу данных:

```sql
CREATE DATABASE spa_db;
CREATE USER user WITH PASSWORD 'password';
GRANT ALL PRIVILEGES ON DATABASE spa_db TO user;
```

---

## 🔙 Backend (FastAPI)

> ⚠️ **ВАЖНО:** Сначала запустите базу данных (см. раздел выше)!

### 1. Переход в директорию backend

```bash
cd backend
```

### 2. Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

1. Скопируйте шаблон:

```bash
# Windows
copy env.template .env

# Linux/Mac
cp env.template .env
```

2. Откройте `.env` и заполните необходимые значения:

```env
# База данных
# Если используете Docker из backend/docker-compose.yml, порт 5434
DATABASE_URL=postgresql://user:password@localhost:5434/spa_db
# Или для локальной установки PostgreSQL (стандартный порт 5432)
# DATABASE_URL=postgresql://user:password@localhost:5432/spa_db

# Безопасность (ОБЯЗАТЕЛЬНО измените в production!)
SECRET_KEY=your-secret-key-change-in-production-minimum-32-characters-long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Режим работы
ENVIRONMENT=development
DEBUG=True

# CORS (для разработки можно оставить *)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,*

# Email (для отправки кодов верификации)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here
EMAIL_FROM=noreply@spa-app.com
EMAIL_USE_TLS=True

# Супер-админ (создается автоматически при первом запуске)
SUPER_ADMIN_EMAIL=admin@example.com
SUPER_ADMIN_PASSWORD=change-me-strong-password

# Admin панель
ADMIN_PANEL_BASE_URL=http://localhost:5173

# YClients (опционально, если используете интеграцию)
YCLIENTS_ENABLED=False
YCLIENTS_COMPANY_ID=
YCLIENTS_API_TOKEN=
YCLIENTS_USER_TOKEN=
```

**⚠️ ВАЖНО:**
- `SECRET_KEY` должен быть длинным и случайным (минимум 32 символа)
- Для Gmail используйте "Пароль приложения", а не обычный пароль
- В production установите `ENVIRONMENT=production` и `DEBUG=False`

### 5. Применение миграций базы данных

```bash
# Применить все миграции
alembic upgrade head

# Проверить текущую версию
alembic current

# Просмотреть историю миграций
alembic history
```

### 6. Запуск сервера

**Режим разработки (с автоперезагрузкой):**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Production режим:**
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### 7. Проверка работы backend

Откройте в браузере:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Health check**: http://localhost:8000/api/v1/health (если есть)

Вы должны увидеть документацию API.

---

## 📱 Flutter приложение

### 1. Переход в директорию spa

```bash
cd spa
```

### 2. Установка зависимостей

```bash
flutter pub get
```

### 3. Настройка конфигурации

Файл `lib/utils/constants.dart` использует переменные окружения. Вы можете настроить URL backend двумя способами:

**Способ 1: Через переменные окружения при запуске**

```bash
# Android эмулятор
flutter run --dart-define=SERVER_BASE_URL=http://10.0.2.2:8000

# iOS симулятор
flutter run --dart-define=SERVER_BASE_URL=http://localhost:8000

# Физическое устройство (замените на IP вашего компьютера)
flutter run --dart-define=SERVER_BASE_URL=http://192.168.1.100:8000
```

**Способ 2: Изменить значение по умолчанию в коде**

Откройте `lib/utils/constants.dart` и измените:

```dart
static const String baseUrl = String.fromEnvironment(
  'SERVER_BASE_URL',
  defaultValue: 'http://10.0.2.2:8000',  // Для Android эмулятора
  // или 'http://localhost:8000' для iOS
  // или 'http://192.168.1.100:8000' для физического устройства
);
```

**⚠️ ВАЖНО для мобильных устройств:**
- Для **Android эмулятора** используйте `http://10.0.2.2:8000`
- Для **iOS симулятора** используйте `http://localhost:8000`
- Для **физических устройств** используйте IP-адрес вашего компьютера в локальной сети (например, `http://192.168.1.100:8000`)

### 4. Настройка Firebase (если используется)

1. Создайте проект в [Firebase Console](https://console.firebase.google.com/)
2. Добавьте Android/iOS приложения
3. Скачайте конфигурационные файлы:
   - `google-services.json` → `android/app/`
   - `GoogleService-Info.plist` → `ios/Runner/`

### 5. Запуск приложения

**Android:**
```bash
flutter run
# или для конкретного устройства
flutter run -d <device-id>
```

**iOS (только на Mac):**
```bash
flutter run
```

**Список доступных устройств:**
```bash
flutter devices
```

### 6. Сборка для production

**Android APK:**
```bash
flutter build apk --release
```

**Android App Bundle:**
```bash
flutter build appbundle --release
```

**iOS:**
```bash
flutter build ios --release
```

---

## 🖥️ Admin панель

### 1. Переход в директорию admin

```bash
cd admin
```

### 2. Установка зависимостей

```bash
npm install
# или
yarn install
```

### 3. Настройка переменных окружения

Создайте файл `.env` (если его нет):

```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
```

### 4. Запуск в режиме разработки

```bash
npm run dev
# или
yarn dev
```

Приложение будет доступно по адресу: http://localhost:5173

### 5. Сборка для production

```bash
npm run build
# или
yarn build
```

Собранные файлы будут в папке `dist/`.

---

## ✅ Проверка работоспособности

### 1. Проверка Backend

#### Тест 1: Health Check
```bash
curl http://localhost:8000/docs
```
Должна открыться документация Swagger.

#### Тест 2: Регистрация пользователя
```bash
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123456!",
    "full_name": "Test User"
  }'
```

Ожидаемый ответ:
```json
{
  "message": "Код верификации отправлен на email",
  "verification_code": "123456"
}
```

#### Тест 3: Вход в систему
```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123456!"
  }'
```

Ожидаемый ответ с токенами:
```json
{
  "access_token": "...",
  "refresh_token": "...",
  "token_type": "bearer"
}
```

### 2. Проверка Flutter приложения

1. **Запустите приложение** на эмуляторе/устройстве
2. **Проверьте экраны:**
   - ✅ Экран входа/регистрации
   - ✅ Главный экран с услугами
   - ✅ Детали услуги
   - ✅ Профиль пользователя
   - ✅ Экран бронирований (новый)
   - ✅ Бронирование через YClients

3. **Проверьте функционал:**
   - Регистрация нового пользователя
   - Вход в систему
   - Просмотр услуг
   - Бронирование услуги
   - Просмотр своих бронирований
   - Отмена бронирования

### 3. Проверка Admin панели

1. **Откройте** http://localhost:5173
2. **Войдите** с учетными данными супер-админа (из `.env`)
3. **Проверьте разделы:**
   - ✅ Пользователи
   - ✅ Услуги
   - ✅ Категории
   - ✅ Бронирования (если не удалено)
   - ✅ Настройки

### 4. Проверка базы данных

Подключитесь к PostgreSQL:

```bash
# Через Docker (порт 5434)
docker exec -it spa_postgres psql -U user -d spa_db

# Или локально (стандартный порт 5432)
psql -U user -d spa_db -h localhost -p 5432
```

Проверьте таблицы:
```sql
-- Список всех таблиц
\dt

-- Количество пользователей
SELECT COUNT(*) FROM users;

-- Количество услуг
SELECT COUNT(*) FROM services;

-- Последние бронирования
SELECT * FROM bookings ORDER BY created_at DESC LIMIT 5;
```

---

## 🔍 Решение проблем

### Проблема: Backend не запускается

**Ошибка: "ModuleNotFoundError"**
```bash
# Убедитесь, что виртуальное окружение активировано
# Переустановите зависимости
pip install -r requirements.txt
```

**Ошибка: "Connection refused" к базе данных**
- Проверьте, что PostgreSQL запущен
- Проверьте `DATABASE_URL` в `.env` (для Docker используйте порт 5434, для локальной установки - 5432)
- Убедитесь, что порт свободен
- Для Docker: проверьте `docker ps` и убедитесь, что контейнер `spa_postgres` запущен

**Ошибка: "Alembic: Can't locate revision"**
```bash
# Проверьте текущую версию
alembic current

# Примените миграции заново
alembic upgrade head
```

### Проблема: Flutter не подключается к backend

**Ошибка: "Connection refused"**
- Проверьте, что backend запущен на порту 8000
- Для эмулятора Android используйте `10.0.2.2:8000`
- Для физического устройства используйте IP вашего компьютера
- Убедитесь, что firewall не блокирует соединение

**Ошибка: "CORS policy"**
- Проверьте настройки CORS в `backend/app/core/config.py`
- Убедитесь, что ваш URL добавлен в `CORS_ORIGINS`

### Проблема: Миграции не применяются

```bash
# Проверьте подключение к БД
alembic current

# Создайте новую миграцию вручную
alembic revision --autogenerate -m "Описание изменений"

# Примените миграции
alembic upgrade head

# Если нужно откатить
alembic downgrade -1
```

### Проблема: Email не отправляется

- Проверьте настройки SMTP в `.env`
- Для Gmail используйте "Пароль приложения", а не обычный пароль
- Проверьте, что `EMAIL_USE_TLS=True` для порта 587
- Проверьте логи backend на наличие ошибок

### Проблема: Admin панель не подключается к API

- Проверьте, что backend запущен
- Проверьте `VITE_API_BASE_URL` в `.env` файле admin панели
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что CORS настроен правильно

---

## 📝 Чек-лист развертывания

### Backend
- [ ] PostgreSQL установлен и запущен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан и заполнен
- [ ] Миграции применены (`alembic upgrade head`)
- [ ] Backend запущен и доступен на http://localhost:8000
- [ ] Swagger документация открывается

### Flutter
- [ ] Flutter SDK установлен (версия 3.9+)
- [ ] Зависимости установлены (`flutter pub get`)
- [ ] URL backend настроен (через переменные окружения или в `constants.dart`)
- [ ] Firebase настроен (если используется)
- [ ] Приложение запускается на эмуляторе/устройстве
- [ ] Приложение подключается к backend

### Admin панель
- [ ] Node.js установлен (версия 18+)
- [ ] Зависимости установлены (`npm install`)
- [ ] Файл `.env` создан с правильным API URL
- [ ] Admin панель запущена на http://localhost:5173
- [ ] Можно войти с учетными данными супер-админа

### Функциональность
- [ ] Регистрация пользователя работает
- [ ] Вход в систему работает
- [ ] Услуги отображаются
- [ ] Бронирование работает
- [ ] Просмотр бронирований работает
- [ ] Admin панель работает

---

## 🚀 Production развертывание

### Backend

1. **Обновите `.env`:**
```env
ENVIRONMENT=production
DEBUG=False
CORS_ORIGINS=https://yourdomain.com,https://admin.yourdomain.com
SECRET_KEY=<сильный-случайный-ключ-минимум-32-символа>
```

2. **Используйте production сервер:**
```bash
# С Gunicorn
pip install gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

3. **Настройте HTTPS** (через nginx или reverse proxy)

### Flutter

1. **Обновите `constants.dart`:**
```dart
static const String baseUrl = 'https://api.yourdomain.com';
```

2. **Соберите release версию:**
```bash
flutter build apk --release  # Android
flutter build ios --release  # iOS
```

### Admin панель

1. **Соберите проект:**
```bash
npm run build
```

2. **Разверните `dist/` на веб-сервере** (nginx, Apache, или хостинг)

---

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи backend в консоли
2. Проверьте логи Flutter в консоли
3. Проверьте консоль браузера для admin панели
4. Проверьте логи базы данных
5. Убедитесь, что все сервисы запущены

---

**Успешного развертывания! 🎉**

