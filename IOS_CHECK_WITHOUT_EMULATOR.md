# Проверка iOS-совместимости без эмулятора

## ✅ Методы проверки без iOS эмулятора

### 1. Статический анализ кода (Flutter Analyze)

```bash
cd spa
flutter analyze
```

**Что проверяет:**
- Синтаксические ошибки
- Типы данных
- Импорты и зависимости
- Потенциальные проблемы компиляции

**Результат:** Если нет ошибок (только warnings) - код должен компилироваться на iOS.

---

### 2. Проверка зависимостей на iOS-совместимость

```bash
cd spa
flutter pub deps
```

**Проверьте:**
- Все зависимости должны поддерживать iOS
- Особое внимание на:
  - `firebase_core`, `firebase_auth`, `firebase_messaging` ✅
  - `google_sign_in` ✅
  - `shared_preferences` ✅
  - `url_launcher` ✅
  - `webview_flutter` ✅

---

### 3. Проверка iOS-конфигурации

#### 3.1. Info.plist
**Файл:** `spa/ios/Runner/Info.plist`

**Проверьте наличие:**
- ✅ `CFBundleDisplayName` - название приложения
- ✅ `CFBundleIdentifier` - идентификатор приложения
- ✅ `LSRequiresIPhoneOS` - должен быть `true`
- ✅ `UILaunchStoryboardName` - экран запуска

#### 3.2. Podfile
**Файл:** `spa/ios/Podfile`

**Проверьте:**
```ruby
platform :ios, '12.0'  # или выше
```

#### 3.3. Firebase конфигурация
**Файл:** `spa/ios/Runner/GoogleService-Info.plist`

**Проверьте:**
- ✅ Файл должен существовать
- ✅ Должен содержать правильный `PROJECT_ID`
- ✅ Должен содержать `CLIENT_ID` для iOS

---

### 4. Проверка платформо-специфичного кода

#### 4.1. Проверка использования Platform

**Найдено в коде:**
```dart
// spa/lib/services/push_service.dart
if (io.Platform.isAndroid) return 'android';
if (io.Platform.isIOS) return 'ios';
```

✅ **Правильно** - код проверяет обе платформы.

#### 4.2. Проверка условных импортов

**Проверьте файлы:**
- `spa/lib/services/push_service.dart` ✅
- `spa/lib/services/api_service.dart` ✅

**Формат должен быть:**
```dart
import 'dart:io' if (dart.library.html) 'dart:html' as io;
```

---

### 5. Проверка разрешений iOS

#### 5.1. Info.plist - Разрешения

**Добавьте в `Info.plist` (если нужно):**

```xml
<!-- Push Notifications -->
<key>UIBackgroundModes</key>
<array>
    <string>remote-notification</string>
</array>

<!-- Camera (если используется) -->
<key>NSCameraUsageDescription</key>
<string>Для сканирования QR-кодов</string>

<!-- Photo Library (если используется) -->
<key>NSPhotoLibraryUsageDescription</key>
<string>Для выбора фотографий</string>

<!-- Location (если используется) -->
<key>NSLocationWhenInUseUsageDescription</key>
<string>Для определения местоположения</string>
```

#### 5.2. Push Notifications

**Проверьте:**
- ✅ В коде используется `requestPermission()` для iOS
- ✅ В `Info.plist` добавлен `UIBackgroundModes` с `remote-notification`

---

### 6. Проверка сборки (Dry Run)

#### 6.1. Проверка синтаксиса iOS-проекта

```bash
cd spa/ios
pod install  # Если установлен CocoaPods
```

#### 6.2. Проверка Xcode проекта

**Откройте в Xcode (если доступен):**
```bash
open spa/ios/Runner.xcworkspace
```

**Проверьте:**
- ✅ Проект открывается без ошибок
- ✅ Все зависимости подключены
- ✅ Signing & Capabilities настроены

---

### 7. Онлайн-проверка зависимостей

#### 7.1. Проверка на pub.dev

Посетите страницы зависимостей и проверьте:
- ✅ Поддержка iOS указана
- ✅ Минимальная версия iOS
- ✅ Известные проблемы

**Основные зависимости:**
- https://pub.dev/packages/firebase_core
- https://pub.dev/packages/firebase_auth
- https://pub.dev/packages/google_sign_in
- https://pub.dev/packages/firebase_messaging

---

### 8. Проверка через Flutter Doctor

```bash
flutter doctor -v
```

**Проверьте секцию iOS:**
```
[✓] Xcode - develop for iOS and macOS
[✓] CocoaPods - CocoaPods version 1.x.x
```

---

### 9. Тестирование на реальном устройстве (без эмулятора)

#### 9.1. TestFlight (Beta-тестирование)

1. Соберите iOS-версию:
   ```bash
   cd spa
   flutter build ios --release
   ```

2. Загрузите в App Store Connect
3. Добавьте тестеров в TestFlight
4. Тестируйте на реальных устройствах

#### 9.2. Ad-Hoc Distribution

1. Соберите .ipa файл:
   ```bash
   flutter build ipa
   ```

2. Установите через Xcode или через сервисы типа Diawi

---

### 10. Автоматизированная проверка

#### 10.1. Создайте скрипт проверки

**Создайте файл `check_ios.sh`:**

```bash
#!/bin/bash

echo "🔍 Проверка iOS-совместимости..."

# 1. Flutter analyze
echo "1. Запуск flutter analyze..."
flutter analyze

# 2. Проверка зависимостей
echo "2. Проверка зависимостей..."
flutter pub deps | grep -i ios

# 3. Проверка Info.plist
echo "3. Проверка Info.plist..."
if [ -f "ios/Runner/Info.plist" ]; then
    echo "✅ Info.plist найден"
else
    echo "❌ Info.plist не найден"
fi

# 4. Проверка GoogleService-Info.plist
echo "4. Проверка Firebase конфигурации..."
if [ -f "ios/Runner/GoogleService-Info.plist" ]; then
    echo "✅ GoogleService-Info.plist найден"
else
    echo "⚠️ GoogleService-Info.plist не найден (может быть нормально для теста)"
fi

# 5. Проверка Podfile
echo "5. Проверка Podfile..."
if [ -f "ios/Podfile" ]; then
    echo "✅ Podfile найден"
    grep -i "platform :ios" ios/Podfile
else
    echo "❌ Podfile не найден"
fi

echo "✅ Проверка завершена!"
```

---

## 🚨 Частые проблемы и решения

### Проблема 1: Отсутствует GoogleService-Info.plist
**Решение:** Скопируйте файл из Firebase Console в `spa/ios/Runner/`

### Проблема 2: Неправильный Bundle Identifier
**Решение:** Проверьте в Xcode: Runner → Signing & Capabilities

### Проблема 3: Проблемы с CocoaPods
**Решение:**
```bash
cd spa/ios
pod deintegrate
pod install
```

### Проблема 4: Минимальная версия iOS
**Решение:** Обновите в `Podfile`:
```ruby
platform :ios, '12.0'  # или выше
```

---

## ✅ Чек-лист перед публикацией

- [ ] `flutter analyze` не показывает ошибок
- [ ] Все зависимости поддерживают iOS
- [ ] `Info.plist` содержит все необходимые разрешения
- [ ] `GoogleService-Info.plist` настроен правильно
- [ ] `Podfile` указывает правильную версию iOS
- [ ] Bundle Identifier уникален
- [ ] Иконки приложения настроены
- [ ] Splash screen настроен
- [ ] Push notifications настроены
- [ ] Проект открывается в Xcode без ошибок

---

## 📱 Альтернативные методы тестирования

### 1. Codemagic / GitHub Actions
Настройте CI/CD для автоматической сборки iOS:
- Автоматическая проверка компиляции
- Автоматические тесты
- Сборка для TestFlight

### 2. Appetize.io
Онлайн-эмулятор iOS (платный):
- Загрузите .ipa файл
- Тестируйте в браузере

### 3. Firebase App Distribution
Бета-тестирование через Firebase:
- Простая загрузка
- Автоматические уведомления тестерам

---

## 🎯 Рекомендации

1. **Всегда проверяйте `flutter analyze`** перед коммитом
2. **Используйте TestFlight** для тестирования на реальных устройствах
3. **Проверяйте зависимости** на pub.dev перед добавлением
4. **Документируйте iOS-специфичные настройки**
5. **Используйте CI/CD** для автоматической проверки

---

**Примечание:** Полная уверенность в работоспособности на iOS возможна только при тестировании на реальном устройстве или эмуляторе. Но эти методы помогут выявить большинство проблем заранее.

