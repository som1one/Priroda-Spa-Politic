# YClients виджет - выбор мастера и как это работает

## 🎯 Ответ на вопрос

**ДА, в виджете YClients ЕСТЬ выбор мастера!**

Когда пользователь открывает виджет YClients через WebView, он видит:
- ✅ Выбор услуги (уже выбрана, если передали `services={id}`)
- ✅ **Выбор мастера** (если в YClients настроено несколько мастеров для услуги)
- ✅ Выбор даты и времени (реальные доступные слоты)
- ✅ Цены (динамические, могут зависеть от мастера)
- ✅ Дополнительные услуги (если настроены)

---

## 🔄 Как это работает сейчас

### Текущая реализация:

```
1. Пользователь выбирает услугу в нашем приложении
   ↓
2. Проверяется yclients_service_id
   ↓
3. Открывается YClientsBookingScreen (WebView)
   ↓
4. Загружается URL: https://yclients.com/timetable/{company_id}?services={service_id}
   ↓
5. В виджете YClients пользователь видит:
   - Услуга уже выбрана
   - Список мастеров (если несколько)
   - Календарь с доступными датами
   - Временные слоты (реальные, из расписания)
   - Цены (могут отличаться у разных мастеров)
   ↓
6. Пользователь выбирает мастера, дату, время
   ↓
7. Заполняет контакты и бронирует
   ↓
8. Возврат в наше приложение
```

---

## 📱 YPlaces vs Виджет YClients

### YPlaces
- Это **отдельное мобильное приложение** от YClients
- Клиенты устанавливают его на телефон
- Там тоже есть выбор мастера и бронирование
- **НО это НЕ то, что мы используем!**

### Виджет YClients (что мы используем)
- Это **веб-страница** для онлайн-записи
- Встраивается через WebView в наше приложение
- Работает как обычный сайт
- **В нём ЕСТЬ выбор мастера**, если настроено в YClients

---

## ⚙️ Настройка выбора мастера в YClients

### В личном кабинете YClients:

1. **Настройки → Услуги**
   - Выберите услугу
   - Убедитесь, что услуга доступна для онлайн-записи

2. **Настройки → Мастера**
   - Убедитесь, что мастера привязаны к услугам
   - Настройте, могут ли клиенты выбирать мастера

3. **Настройки → Онлайн-запись**
   - Включите возможность выбора мастера
   - Настройте, показывать ли всех мастеров или только доступных

### В нашем приложении:

**Вариант 1: Выбор мастера в виджете (рекомендуется)**
- Не указываем `yclients_staff_id` в услуге
- Виджет покажет список всех мастеров для услуги
- Пользователь выберет сам

**Вариант 2: Фиксированный мастер**
- Указываем `yclients_staff_id` в услуге
- Виджет сразу покажет этого мастера
- Выбора не будет (но можно убрать `staff` из URL)

---

## 🔧 Текущая реализация

### URL виджета:

```python
# Если мастер НЕ указан - будет выбор
https://yclients.com/timetable/123456?services=789

# Если мастер указан - будет привязан к нему
https://yclients.com/timetable/123456?services=789&staff=456
```

### Код в backend:

```python
widget_url = f"https://yclients.com/timetable/{company_id}"

if service.yclients_service_id:
    widget_url += f"?services={service.yclients_service_id}"

# Если указан мастер - привязываем
# Если НЕ указан - виджет покажет выбор мастера
if service.yclients_staff_id:
    widget_url += f"&staff={service.yclients_staff_id}"
```

---

## ✅ Что видит пользователь в виджете

### Если мастер НЕ указан (`yclients_staff_id = null`):

1. **Выбор мастера** (если несколько)
   - Список мастеров с фото
   - Рейтинги и отзывы
   - Можно выбрать любого

2. **Выбор даты**
   - Календарь с доступными датами
   - Только свободные дни

3. **Выбор времени**
   - Реальные доступные слоты
   - Учитывается расписание мастера

4. **Цена**
   - Может отличаться у разных мастеров
   - Показывается после выбора мастера

### Если мастер указан (`yclients_staff_id = 456`):

1. Мастер уже выбран (пропускается шаг выбора)
2. Показывается только его расписание
3. Цена этого мастера

---

## 🎨 Рекомендации

### Для максимальной гибкости:

**Не указывайте `yclients_staff_id` в услуге** - тогда пользователь сможет выбрать мастера в виджете.

**Исключение:** Если услугу делает только один мастер - можно указать его ID.

### Пример настройки:

```sql
-- Услуга с выбором мастера
UPDATE services 
SET yclients_service_id = 789, 
    yclients_staff_id = NULL  -- NULL = выбор мастера
WHERE id = 1;

-- Услуга с фиксированным мастером
UPDATE services 
SET yclients_service_id = 789, 
    yclients_staff_id = 456  -- Конкретный мастер
WHERE id = 2;
```

---

## 📋 Итого

**Вопрос:** Есть ли выбор мастера в YClients?

**Ответ:** 
- ✅ **ДА, есть!** В виджете YClients автоматически показывается выбор мастера
- ✅ Работает через WebView в нашем приложении
- ✅ Не нужно ничего дополнительно настраивать в коде
- ✅ Достаточно настроить в личном кабинете YClients

**YPlaces:**
- Это отдельное приложение, мы его не используем
- Мы используем веб-виджет YClients через WebView

**Что нужно сделать:**
1. Настроить в YClients возможность выбора мастера
2. Не указывать `yclients_staff_id` (или указать NULL) - чтобы был выбор
3. Готово! Виджет сам покажет выбор мастера

---

## 🧪 Как проверить

1. Откройте услугу с `yclients_service_id`, но БЕЗ `yclients_staff_id`
2. Нажмите "Быстрое бронирование"
3. В WebView должен открыться виджет YClients
4. Должен быть шаг "Выбор мастера" (если в YClients настроено несколько мастеров)

Если выбора мастера нет:
- Проверьте настройки в YClients (Онлайн-запись → Выбор мастера)
- Убедитесь, что к услуге привязано несколько мастеров
- Обратитесь в поддержку YClients для настройки

