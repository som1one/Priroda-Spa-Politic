# Быстрая настройка VK ID

## Шаг 1: Получение токенов от VK

### 1. Перейдите на https://id.vk.com/business

### 2. Войдите в свой аккаунт VK

### 3. Создайте новое приложение:
   - Нажмите "Создать приложение"
   - Выберите тип: **"Веб-сайт"** или **"Мобильное приложение"**
   - Заполните название приложения
   - Нажмите "Создать"

### 4. После создания приложения вы увидите:
   - **ID приложения** (App ID) - это число, например: `12345678`
   - **Защищенный ключ** (Secret Key) - это строка, например: `AbCdEfGhIjKlMnOpQrStUvWxYz`

### 5. Настройте Redirect URI:
   - В настройках приложения найдите раздел "Redirect URI"
   - Добавьте: `https://oauth.vk.com/blank.html`
   - Сохраните изменения

## Шаг 2: Настройка бэкенда

Откройте файл `backend/.env` и добавьте:

```env
VK_APP_ID=12345678
VK_SECRET_KEY=AbCdEfGhIjKlMnOpQrStUvWxYz
```

**Замените** `12345678` и `AbCdEfGhIjKlMnOpQrStUvWxYz` на ваши реальные значения!

## Шаг 3: Настройка Flutter приложения

### Вариант A: Через переменную окружения (рекомендуется)

При запуске приложения:
```bash
flutter run --dart-define=VK_APP_ID=12345678
```

### Вариант B: Прямо в коде

Откройте `spa/lib/utils/constants.dart` и найдите строку:
```dart
static const String vkAppId = String.fromEnvironment(
  'VK_APP_ID',
  defaultValue: '', // TODO: Установить VK App ID
);
```

Замените на:
```dart
static const String vkAppId = String.fromEnvironment(
  'VK_APP_ID',
  defaultValue: '12345678', // Ваш VK App ID
);
```

## Шаг 4: Перезапуск

1. Перезапустите бэкенд (если он запущен)
2. Перезапустите Flutter приложение

## Готово! 🎉

Теперь кнопка "Войти с помощью VK" должна работать.

## Проверка

1. Откройте приложение
2. Нажмите "Войти с помощью VK"
3. Должна открыться страница авторизации VK
4. После входа вы будете перенаправлены в приложение

## Если что-то не работает

### Ошибка "VK App ID не настроен"
- Проверьте, что `VK_APP_ID` установлен в `constants.dart` или через `--dart-define`

### Ошибка "Неверный VK токен" на бэкенде
- Проверьте, что `VK_SECRET_KEY` правильно установлен в `backend/.env`
- Убедитесь, что вы скопировали ключ полностью (без пробелов)

### WebView не открывается
- Проверьте подключение к интернету
- Убедитесь, что `webview_flutter` установлен: `flutter pub get`

