# Настройка системы записи через YClients

## ✅ Что уже реализовано

### Backend
- ✅ Endpoint `GET /api/v1/yclients/widget-url/{service_id}` - получение URL виджета с предзаполнением
- ✅ Endpoint `POST /api/v1/yclients/sync-bookings` - синхронизация записей из YClients
- ✅ Endpoint `POST /api/v1/yclients/webhook` - обработка webhook от YClients
- ✅ Автоматическая синхронизация через APScheduler (каждые 30 минут)
- ✅ Сопоставление пользователей по email и телефону
- ✅ Обработка отмены записей из YClients

### Flutter
- ✅ Экран `YClientsBookingScreen` - отображение виджета YClients в WebView
- ✅ Отслеживание успешного бронирования
- ✅ Диалог успеха после бронирования
- ✅ Интеграция в `ServiceDetailScreen` - автоматический выбор метода записи
- ✅ Улучшенное отображение записей из YClients в `BookingsScreen`
- ✅ Индикатор записей из YClients

---

## 📋 Шаги настройки

### 1. Проверка конфигурации YClients

Запустите скрипт проверки:

```bash
cd backend
python scripts/check_yclients_config.py
```

Скрипт проверит:
- ✅ YCLIENTS_ENABLED
- ✅ YCLIENTS_COMPANY_ID
- ✅ YCLIENTS_API_TOKEN
- ✅ YCLIENTS_USER_TOKEN
- ✅ Подключение к YClients API

### 2. Настройка .env файла

Убедитесь, что в `backend/.env` установлены:

```env
YCLIENTS_ENABLED=True
YCLIENTS_COMPANY_ID=235564
YCLIENTS_API_TOKEN=your-api-token
YCLIENTS_USER_TOKEN=your-user-token
```

**Как получить токены:**
- **API Token (Partner Token)**: Настройки → Интеграции → Партнёрские приложения
- **User Token**: Используйте `python scripts/get_yclients_user_token.py`

### 3. Синхронизация услуг и мастеров

Запустите синхронизацию каталога:

**Windows:**
```bash
cd backend
run_sync_catalog.bat
```

**Linux/Mac:**
```bash
cd backend
chmod +x scripts/run_sync_catalog.sh
./scripts/run_sync_catalog.sh
```

**Или вручную:**
```bash
cd backend
python scripts/sync_yclients_catalog.py
```

Это создаст/обновит:
- Услуги из YClients (с `yclients_service_id`)
- Мастеров из YClients (с `yclients_staff_id`)
- Расписание мастеров

**Проверка результатов:**
```sql
-- Проверьте количество синхронизированных услуг
SELECT COUNT(*) FROM services WHERE yclients_service_id IS NOT NULL;

-- Проверьте количество синхронизированных мастеров
SELECT COUNT(*) FROM staff WHERE yclients_staff_id IS NOT NULL;
```

### 4. Настройка webhook в YClients

**Автоматическая помощь:**
```bash
cd backend
python scripts/setup_webhook_guide.py
```

Скрипт поможет:
- Сформировать правильный URL для webhook
- Показать пошаговую инструкцию
- Сохранить информацию в файл

**Ручная настройка:**

1. Войдите в админ-панель YClients
2. Перейдите: **Настройки → Интеграции → Webhooks**
3. Добавьте новый webhook:
   - **URL**: `https://your-domain.com/api/v1/yclients/webhook`
   - **События**: Все изменения записей
   - **Метод**: POST

**Важно:** 
- Замените `your-domain.com` на ваш реальный домен!
- URL должен быть доступен из интернета (для production)
- Используйте HTTPS в production

**Проверка webhook:**
```bash
# После настройки протестируйте
python scripts/test_yclients_webhook.py
```

### 5. Проверка работы

#### Тест 1: Получение URL виджета
```bash
curl -X GET "http://localhost:9003/api/v1/yclients/widget-url/1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Должен вернуть:
```json
{
  "widget_url": "https://yclients.com/timetable/235564?services=...",
  "service_id": 1,
  "service_name": "Название услуги"
}
```

#### Тест 2: Синхронизация записей
```bash
curl -X POST "http://localhost:9003/api/v1/yclients/sync-bookings" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### Тест 3: Проверка в приложении
1. Откройте услугу в приложении
2. Нажмите "Записаться"
3. Должен открыться виджет YClients
4. Заполните форму и создайте запись
5. После успешного бронирования должен появиться диалог успеха
6. Проверьте список записей - новая запись должна отображаться

---

## 🔄 Как это работает

### Поток бронирования

```
1. Пользователь выбирает услугу
   ↓
2. ServiceDetailScreen проверяет yclientsServiceId
   ↓
3. Если услуга привязана к YClients:
   → Открывается YClientsBookingScreen
   → Загружается URL виджета через API
   → Отображается WebView с виджетом YClients
   ↓
4. Пользователь в виджете:
   - Выбирает мастера
   - Выбирает дату и время
   - Проверяет/редактирует данные
   - Оплачивает (в виджете)
   ↓
5. YClients создает запись
   ↓
6. Webhook → POST /api/v1/yclients/webhook
   ↓
7. Backend синхронизирует запись в локальную БД
   ↓
8. Flutter отслеживает успех и показывает диалог
   ↓
9. Пользователь видит запись в списке
```

### Синхронизация

**Автоматическая:**
- Каждые 30 минут через APScheduler
- Синхронизируются записи на ближайшие 30 дней

**Через webhook:**
- Мгновенно при изменениях в YClients
- Обрабатываются события: создание, изменение статуса, отмена

**Вручную:**
- Через API: `POST /api/v1/yclients/sync-bookings`

### Сопоставление пользователей

При синхронизации записи сопоставляются с пользователями:
1. По email (точное совпадение)
2. По телефону (нормализация и поиск по последним 10 цифрам)

Если пользователь не найден, запись пропускается (логируется предупреждение).

---

## 🐛 Решение проблем

### Проблема: "Услуга не привязана к YClients"

**Решение:**
1. Запустите синхронизацию: `python scripts/sync_yclients_catalog.py`
2. Проверьте в админке, что у услуги есть `yclients_service_id`
3. Если услуги нет в YClients, создайте её там и синхронизируйте снова

### Проблема: "YClients недоступен"

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте токены в `.env`
3. Запустите `python scripts/check_yclients_config.py`
4. Проверьте логи backend на ошибки

### Проблема: Записи не синхронизируются

**Решение:**
1. Проверьте webhook в YClients (правильный URL)
2. Проверьте логи backend на ошибки webhook
3. Запустите ручную синхронизацию: `POST /api/v1/yclients/sync-bookings`
4. Проверьте, что пользователи найдены (по email/телефону)

### Проблема: Виджет не загружается

**Решение:**
1. Проверьте, что endpoint `/api/v1/yclients/widget-url/{service_id}` работает
2. Проверьте логи Flutter на ошибки
3. Проверьте, что услуга привязана к YClients
4. Проверьте интернет-соединение

---

## 📊 Мониторинг

### Логи

Проверяйте логи backend для диагностики:
- Синхронизация: `INFO: Синхронизация завершена: создано X, обновлено Y`
- Webhook: `INFO: Получен webhook от YClients`
- Ошибки: `ERROR: Ошибка синхронизации...`

### Метрики

Отслеживайте:
- Количество синхронизированных записей
- Количество пропущенных записей (пользователь не найден)
- Ошибки синхронизации
- Ошибки webhook

---

## ✅ Чеклист готовности

- [ ] YClients аккаунт настроен
- [ ] Токены получены и установлены в `.env`
- [ ] Конфигурация проверена (`check_yclients_config.py`)
- [ ] Услуги и мастера синхронизированы
- [ ] Webhook настроен в YClients
- [ ] APScheduler запущен (проверьте логи при старте)
- [ ] Виджет открывается в приложении
- [ ] Записи создаются через виджет
- [ ] Записи отображаются в списке
- [ ] Синхронизация работает (проверьте через 30 минут)

---

## 📝 Дополнительные настройки

### Изменение интервала синхронизации

В `backend/app/main.py` измените:
```python
trigger=IntervalTrigger(minutes=30)  # Измените на нужный интервал
```

### Отключение автоматической синхронизации

В `backend/app/main.py` закомментируйте:
```python
# scheduler.add_job(...)
```

### Ручная синхронизация

Используйте API:
```bash
curl -X POST "http://localhost:9003/api/v1/yclients/sync-bookings" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 🔗 Связанные документы

- `BOOKING_SOLUTIONS.md` - Описание вариантов реализации
- `YCLIENTS_INTEGRATION.md` - Документация по интеграции YClients
- `YCLIENTS_SETUP_COMPLETE.md` - Полная инструкция по настройке

---

## 💡 Советы

1. **Тестирование**: Сначала протестируйте на одной услуге
2. **Мониторинг**: Следите за логами первые несколько дней
3. **Резервное копирование**: Регулярно делайте бэкап БД
4. **Обновления**: Синхронизируйте услуги после изменений в YClients

---

## 🎉 Готово!

После выполнения всех шагов система записи через YClients будет полностью настроена и готова к использованию!

