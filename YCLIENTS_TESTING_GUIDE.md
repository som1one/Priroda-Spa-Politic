# Руководство по тестированию YClients интеграции

## 🧪 Тестовые скрипты

### 1. Тест конфигурации

Проверяет настройки YClients и подключение к API:

```bash
cd backend
python scripts/check_yclients_config.py
```

**Что проверяет:**
- ✅ YCLIENTS_ENABLED
- ✅ YCLIENTS_COMPANY_ID
- ✅ YCLIENTS_API_TOKEN
- ✅ YCLIENTS_USER_TOKEN
- ✅ Подключение к YClients API
- ✅ Получение услуг

**Ожидаемый результат:**
```
✅ Конфигурация YClients проверена успешно!
```

---

### 2. Тест синхронизации записей

Тестирует получение и синхронизацию записей из YClients:

```bash
cd backend
python scripts/test_yclients_sync.py
```

**Что проверяет:**
- ✅ Получение записей из YClients API
- ✅ Синхронизация записей в локальную БД
- ✅ Сопоставление пользователей
- ✅ Обработка ошибок

**Ожидаемый результат:**
```
✅ Синхронизация завершена
```

**Проверка результатов:**
```sql
-- Проверьте синхронизированные записи
SELECT id, service_name, appointment_datetime, status, notes 
FROM bookings 
WHERE notes LIKE '%YClients%' 
ORDER BY created_at DESC 
LIMIT 10;
```

---

### 3. Тест webhook

Имитирует webhook от YClients для проверки обработки:

```bash
cd backend
python scripts/test_yclients_webhook.py
```

**⚠️ ВАЖНО:** Перед запуском:
1. Убедитесь, что backend запущен
2. Замените в скрипте:
   - `id` на реальный ID записи из YClients (если есть)
   - `email` или `phone` на данные существующего пользователя

**Что проверяет:**
- ✅ Получение webhook
- ✅ Обработка данных записи
- ✅ Создание/обновление записи в БД
- ✅ Сопоставление пользователей

**Ожидаемый результат:**
```
✅ Webhook успешно обработан!
```

---

## 📋 Полное тестирование системы

### Шаг 1: Проверка конфигурации

```bash
python scripts/check_yclients_config.py
```

Если есть ошибки - исправьте их в `.env`.

### Шаг 2: Синхронизация каталога

```bash
python scripts/sync_yclients_catalog.py
```

Проверьте, что услуги и мастера синхронизированы:
```sql
-- Проверьте услуги с YClients ID
SELECT id, name, yclients_service_id 
FROM services 
WHERE yclients_service_id IS NOT NULL 
LIMIT 10;

-- Проверьте мастеров с YClients ID
SELECT id, name, yclients_staff_id 
FROM staff 
WHERE yclients_staff_id IS NOT NULL 
LIMIT 10;
```

### Шаг 3: Тест синхронизации записей

```bash
python scripts/test_yclients_sync.py
```

### Шаг 4: Тест виджета в приложении

1. Откройте приложение
2. Выберите услугу с `yclients_service_id`
3. Нажмите "Записаться"
4. Должен открыться виджет YClients
5. Создайте тестовую запись
6. Проверьте, что появился диалог успеха

### Шаг 5: Проверка синхронизации записи

После создания записи через виджет:

1. **Автоматическая синхронизация:**
   - Подождите до 30 минут (или запустите вручную)
   - Проверьте БД:
   ```sql
   SELECT * FROM bookings 
   WHERE notes LIKE '%YClients%' 
   ORDER BY created_at DESC 
   LIMIT 1;
   ```

2. **Ручная синхронизация:**
   ```bash
   curl -X POST "http://localhost:9003/api/v1/yclients/sync-bookings" \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

3. **Через webhook:**
   - Если webhook настроен, запись должна синхронизироваться мгновенно
   - Проверьте логи backend на наличие webhook запросов

### Шаг 6: Тест отмены записи

1. Откройте список записей в приложении
2. Найдите запись из YClients (с индикатором)
3. Нажмите "Отменить"
4. Проверьте, что запись отменена в БД
5. Проверьте логи backend - должна быть попытка отменить в YClients

---

## 🔍 Проверка логов

### Backend логи

Проверьте логи backend на наличие:

**Успешная синхронизация:**
```
INFO: Синхронизация завершена: создано X, обновлено Y, пропущено Z
```

**Webhook:**
```
INFO: Получен webhook от YClients: {...}
INFO: Запись {id} успешно синхронизирована через webhook
```

**Ошибки:**
```
ERROR: Ошибка синхронизации записи {id}: ...
WARNING: Пользователь не найден для записи {id}
```

### Flutter логи

Проверьте логи Flutter на наличие:

**Успешная загрузка виджета:**
```
✅ URL виджета получен
```

**Ошибки:**
```
❌ Ошибка загрузки виджета: ...
```

---

## 🐛 Решение проблем

### Проблема: Синхронизация не работает

**Проверьте:**
1. YCLIENTS_ENABLED = True
2. Токены правильные
3. Подключение к интернету
4. Логи backend на ошибки

**Решение:**
```bash
# Проверьте конфигурацию
python scripts/check_yclients_config.py

# Проверьте подключение
python scripts/test_yclients_sync.py
```

### Проблема: Записи не синхронизируются

**Проверьте:**
1. Пользователь найден (по email/телефону)
2. Дата/время корректные
3. Логи на ошибки парсинга

**Решение:**
```sql
-- Проверьте пользователей
SELECT id, email, phone FROM users LIMIT 10;

-- Проверьте записи из YClients
SELECT * FROM bookings WHERE notes LIKE '%YClients%';
```

### Проблема: Webhook не работает

**Проверьте:**
1. Webhook настроен в YClients
2. URL правильный и доступен
3. Backend запущен
4. Логи backend на webhook запросы

**Решение:**
```bash
# Тест webhook
python scripts/test_yclients_webhook.py

# Проверьте логи
tail -f backend/logs/app.log | grep webhook
```

### Проблема: Виджет не загружается

**Проверьте:**
1. Услуга привязана к YClients (`yclients_service_id`)
2. Endpoint `/api/v1/yclients/widget-url/{service_id}` работает
3. Интернет-соединение
4. Логи Flutter

**Решение:**
```bash
# Проверьте endpoint
curl -X GET "http://localhost:9003/api/v1/yclients/widget-url/1" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## ✅ Чеклист тестирования

- [ ] Конфигурация проверена (`check_yclients_config.py`)
- [ ] Каталог синхронизирован (`sync_yclients_catalog.py`)
- [ ] Синхронизация записей работает (`test_yclients_sync.py`)
- [ ] Виджет открывается в приложении
- [ ] Запись создается через виджет
- [ ] Запись отображается в списке
- [ ] Запись синхронизируется в БД
- [ ] Webhook работает (если настроен)
- [ ] Отмена записи работает
- [ ] Логи не содержат критических ошибок

---

## 📊 Мониторинг

### Метрики для отслеживания

1. **Количество синхронизированных записей:**
   ```sql
   SELECT COUNT(*) FROM bookings WHERE notes LIKE '%YClients%';
   ```

2. **Количество пропущенных записей:**
   - Проверьте логи на "Пользователь не найден"

3. **Ошибки синхронизации:**
   - Проверьте логи на "Ошибка синхронизации"

4. **Время синхронизации:**
   - Проверьте логи на время выполнения

---

## 🎯 Следующие шаги

После успешного тестирования:

1. ✅ Настройте webhook в YClients (если еще не настроен)
2. ✅ Проверьте работу в production
3. ✅ Настройте мониторинг
4. ✅ Документируйте процесс для команды

---

## 💡 Советы

1. **Тестируйте постепенно:** Сначала конфигурация, потом синхронизация, потом виджет
2. **Проверяйте логи:** Всегда смотрите логи при проблемах
3. **Используйте тестовые данные:** Создайте тестового пользователя для проверки
4. **Мониторьте производительность:** Следите за временем синхронизации

---

## 📝 Дополнительные ресурсы

- `YCLIENTS_BOOKING_SETUP.md` - Полная инструкция по настройке
- `BOOKING_SOLUTIONS.md` - Описание вариантов реализации
- `YCLIENTS_INTEGRATION.md` - Документация по интеграции

