# Детальное описание процесса бронирования

## 📋 Обзор

Все бронирования услуг происходят через **YClients** виджет. Пользователь выбирает услугу в приложении, затем переходит в виджет YClients, где происходит выбор мастера, даты, времени и оплата.

---

## 🗺️ Схема процесса бронирования

```
1. Главный экран (HomeScreen)
   ↓
2. Меню услуг (MenuSpaScreen)
   ↓
3. Детали услуги (ServiceDetailScreen)
   ↓
4. YClients виджет (YClientsBookingScreen) ⭐ ОСНОВНОЙ ЭКРАН
   ↓
5. Успешное бронирование → Список бронирований (BookingsScreen)
```

---

## 📱 Подробное описание экранов и этапов

### 1️⃣ **MenuSpaScreen** - Меню услуг

**Путь:** `/menu-spa`  
**Файл:** `spa/lib/screens/menu/menu_spa_screen.dart`

**Что происходит:**
- Пользователь видит категории услуг
- Может перейти в категорию и увидеть услуги в ней
- Есть поиск по услугам
- Есть хлебные крошки для навигации по категориям
- При выборе услуги → переход на `ServiceDetailScreen`

**Элементы:**
- Поисковая строка
- Категории услуг (карточки с изображениями)
- Список услуг в категории
- Хлебные крошки для навигации

**Навигация:**
- Выбор услуги → `ServiceDetailScreen` (с `serviceId`)

---

### 2️⃣ **ServiceDetailScreen** - Детали услуги

**Путь:** `/service-detail`  
**Файл:** `spa/lib/screens/service/service_detail_screen.dart`

**Что происходит:**
- Показываются детали услуги:
  - Изображение
  - Название
  - Описание
  - Продолжительность
  - Цена
  - Основные моменты (highlights)
  - Дополнительные услуги
- Есть кнопка "Записаться" (фиксированная внизу)
- При нажатии на "Записаться" → переход на `YClientsBookingScreen`

**Метод:** `_handleQuickBooking()`
```dart
Future<void> _handleQuickBooking() async {
  if (_service == null) return;
  
  // Все услуги бронируются через YClients
  final result = await Navigator.of(context).pushNamed(
    RouteNames.yclientsBooking,
    arguments: {
      'serviceId': _service!.id,
      'service': _service,
    },
  );
}
```

**Навигация:**
- Кнопка "Записаться" → `YClientsBookingScreen` (с `serviceId` и `service`)

---

### 3️⃣ **YClientsBookingScreen** - YClients виджет (ОСНОВНОЙ ЭКРАН)

**Путь:** `/yclients-booking`  
**Файл:** `spa/lib/screens/booking/yclients_booking_screen.dart`

**Это основной экран бронирования!** Здесь происходит весь процесс.

#### 🔧 Технические детали:

**Что происходит:**
1. **Получение URL виджета:**
   - Вызывается API: `GET /api/v1/yclients/widget-url/{serviceId}`
   - Backend формирует URL виджета YClients с параметрами:
     - `services={yclients_service_id}` - ID услуги в YClients
     - `staff={yclients_staff_id}` - ID мастера (опционально)
     - `client_name={name} {surname}` - Имя пользователя (предзаполнение)
     - `client_email={email}` - Email пользователя (предзаполнение)
     - `client_phone={phone}` - Телефон пользователя (предзаполнение)

2. **Загрузка виджета:**
   - WebView загружает URL виджета YClients
   - Показывается SkeletonLoader во время загрузки

3. **Пользователь в виджете YClients:**
   - Выбирает мастера (если несколько)
   - Выбирает дату из календаря
   - Выбирает время из доступных слотов
   - Заполняет/проверяет контакты (уже предзаполнены)
   - Выбирает способ оплаты (если есть)
   - Подтверждает бронирование

4. **Оплата:**
   - Оплата происходит прямо в виджете YClients
   - Поддерживаются все способы оплаты YClients

5. **Отслеживание успешного бронирования:**
   - Отслеживаются изменения URL в WebView
   - Проверяются признаки успешного бронирования:
     - `success`, `confirmed`, `completed`
     - `record_id=`, `booking_id=`
     - Параметры `date=` и `time=`

6. **Диалог успеха:**
   - Показывается диалог с сообщением об успехе
   - Две кнопки:
     - "Отлично" → Возврат назад с обновлением списка
     - "Посмотреть записи" → Переход на `BookingsScreen`

#### 📋 Структура экрана:

```dart
Scaffold(
  appBar: AppBar(
    title: 'Забронировать услугу',
    leading: Back button,
  ),
  body: 
    if (error) 
      EmptyState (ошибка)
    else 
      Stack(
        WebViewWidget (YClients виджет),
        if (isLoading) SkeletonLoader,
      ),
)
```

#### 🔄 Обработка успешного бронирования:

```dart
void _checkForBookingSuccess(String url) {
  // Проверяет URL на признаки успешного бронирования
  // Вызывает _handleBookingSuccess() при обнаружении
}

Future<void> _handleBookingSuccess() async {
  // Показывает диалог успеха
  // Закрывает экран с результатом {'bookingCreated': true}
  // Обновляет список бронирований
}
```

#### 📤 Передача данных пользователя:

Backend автоматически добавляет в URL виджета:
- Имя и фамилия пользователя
- Email
- Телефон (с форматированием +7XXXXXXXXXX)

**Формат URL:**
```
https://yclients.com/timetable/{company_id}?services={service_id}&staff={staff_id}&client_name={name}&client_email={email}&client_phone={phone}
```

#### ⚠️ Обработка ошибок:

- Ошибка загрузки URL → `EmptyState` с сообщением об ошибке
- Ошибка WebView → Показ ошибки с кнопкой "Повторить"
- Используется `getErrorMessage()` для детальных сообщений

---

### 4️⃣ **BookingsScreen** - Список всех бронирований

**Путь:** `/bookings`  
**Файл:** `spa/lib/screens/bookings/bookings_screen.dart`

**Что происходит:**
- Показывается список всех бронирований пользователя
- Есть фильтры по статусу:
  - Все
  - Предстоящие
  - Завершенные
  - Отмененные
- Можно отменить бронирование
- Pull-to-refresh для обновления списка

**Загрузка данных:**
- API: `GET /api/v1/bookings?status={status}`
- Показывается `SkeletonLoader` во время загрузки
- При пустом списке → `EmptyState`

**Отображение бронирования:**
- Название услуги
- Статус (цветной бейдж)
- Дата и время
- Длительность
- Цена (если есть)
- Комментарий (если есть)
- Кнопка отмены (если можно отменить)

**Навигация:**
- Доступ с `ProfileScreen` → "Посмотреть все записи"
- После успешного бронирования → автоматический переход с обновлением списка

---

### 5️⃣ **ProfileScreen** - Профиль с предстоящими записями

**Путь:** `/profile`  
**Файл:** `spa/lib/screens/profile/profile_screen.dart`

**Что происходит:**
- Показывается профиль пользователя
- В секции "Предстоящие записи" показываются **первые 2** будущих бронирования
- Можно нажать на запись или "Посмотреть все" → переход на `BookingsScreen`

**Загрузка данных:**
- API: `GET /api/v1/bookings`
- Показывается `SkeletonBookingCard` во время загрузки
- Фильтруются только будущие записи (`appointmentDateTime > now()`)

**Навигация:**
- Нажатие на запись → `BookingsScreen`
- Кнопка "Посмотреть все" → `BookingsScreen`

---

## 🔄 Альтернативные пути (старые экраны)

### ⚠️ **BookingScreen** - Выбор даты и времени (НЕ ИСПОЛЬЗУЕТСЯ)

**Путь:** `/booking`  
**Файл:** `spa/lib/screens/booking/booking_screen.dart`

**Статус:** Это старый экран для собственного календаря. **НЕ используется** для YClients бронирований.

**Что делает:**
- Показывает календарь для выбора даты
- Показывает временные слоты
- Переход на `BookingDetailsScreen`

**Использование:** Не используется в основном флоу бронирования через YClients.

---

### ⚠️ **BookingDetailsScreen** - Детали бронирования (НЕ ИСПОЛЬЗУЕТСЯ)

**Путь:** `/booking-details`  
**Файл:** `spa/lib/screens/booking/booking_details_screen.dart`

**Статус:** Это старый экран для деталей бронирования. **НЕ используется** для YClients бронирований.

**Что делает:**
- Показывает информацию о выбранной дате/времени
- Поля для дополнительных услуг
- Поля для комментариев
- Переход на YClients (теперь используется как альтернатива)

**Использование:** Не используется в основном флоу. В коде есть переход на YClients, но основной путь - через `ServiceDetailScreen`.

---

## 🔗 Backend API Endpoints

### 1. **Получение URL виджета YClients**

**Endpoint:** `GET /api/v1/yclients/widget-url/{service_id}`  
**Файл:** `backend/app/api/v1/yclients.py`

**Что делает:**
- Получает услугу из БД по `service_id`
- Формирует URL виджета YClients
- Добавляет параметры:
  - `services` - ID услуги в YClients
  - `staff` - ID мастера (если указан)
  - `client_name` - Имя пользователя (предзаполнение)
  - `client_email` - Email пользователя (предзаполнение)
  - `client_phone` - Телефон пользователя (предзаполнение, форматирование +7XXXXXXXXXX)

**Ответ:**
```json
{
  "widget_url": "https://yclients.com/timetable/123456?services=789&client_name=...",
  "service_id": 1,
  "service_name": "Массаж"
}
```

---

### 2. **Получение списка бронирований**

**Endpoint:** `GET /api/v1/bookings?status={status}`  
**Файл:** `backend/app/api/v1/endpoints/bookings.py`

**Что делает:**
- Возвращает список бронирований пользователя
- Фильтрация по статусу (опционально)
- Показывает все записи, включая созданные через YClients

---

### 3. **Синхронизация бронирований из YClients**

**Endpoint:** `POST /api/v1/yclients/sync-bookings`  
**Файл:** `backend/app/api/v1/yclients.py`

**Что делает:**
- Получает записи из YClients за период
- Синхронизирует с локальной БД
- Создает/обновляет записи в нашей БД

**Использование:** Автоматическая синхронизация каждые 30 минут + webhook от YClients

---

### 4. **Webhook от YClients**

**Endpoint:** `POST /api/v1/yclients/webhook`  
**Файл:** `backend/app/api/v1/yclients.py`

**Что делает:**
- Принимает уведомления от YClients о событиях:
  - Создание новой записи
  - Изменение статуса записи
  - Отмена записи
- Автоматически синхронизирует изменения

**Настройка:** В YClients нужно указать URL: `https://your-domain.com/api/v1/yclients/webhook`

---

## 📊 Схема данных

### Service (Услуга)
```dart
{
  id: int,
  name: string,
  yclients_service_id: int?,  // ID услуги в YClients
  yclients_staff_id: int?,     // ID мастера в YClients (опционально)
  ...
}
```

### Booking (Бронирование)
```dart
{
  id: int,
  user_id: int,
  service_name: string,
  appointment_datetime: DateTime,
  status: string,  // pending, confirmed, completed, cancelled
  notes: string?,  // Может содержать "Запись через YClients. ID: {id}"
  ...
}
```

---

## 🎯 Полный флоу бронирования (пошагово)

### Шаг 1: Выбор услуги
1. Пользователь на главном экране → Меню услуг
2. Выбирает категорию или ищет услугу
3. Нажимает на услугу

### Шаг 2: Просмотр деталей
1. Открывается экран `ServiceDetailScreen`
2. Пользователь видит:
   - Изображение услуги
   - Описание
   - Цену
   - Длительность
   - Особенности
3. Нажимает кнопку "Записаться"

### Шаг 3: Бронирование в YClients
1. Открывается `YClientsBookingScreen`
2. Загружается URL виджета (показывается SkeletonLoader)
3. Виджет YClients открывается в WebView
4. Форма уже предзаполнена данными пользователя:
   - Имя
   - Email
   - Телефон
5. Пользователь в виджете:
   - Выбирает мастера (если несколько)
   - Выбирает дату из календаря
   - Выбирает время из доступных слотов
   - Проверяет/редактирует контакты
   - Выбирает способ оплаты (если нужно)
   - Подтверждает бронирование
6. Происходит оплата (в виджете YClients)
7. YClients показывает страницу успеха

### Шаг 4: Обработка успеха
1. Приложение отслеживает изменение URL
2. Обнаруживает признаки успешного бронирования
3. Показывает диалог успеха:
   - Иконка ✓
   - Сообщение "Бронирование успешно создано!"
   - Две кнопки:
     - "Отлично" → Возврат назад
     - "Посмотреть записи" → Переход на список бронирований

### Шаг 5: Просмотр записей
1. Если выбрано "Посмотреть записи":
   - Открывается `BookingsScreen`
   - Список автоматически обновляется
   - Показывается новое бронирование
2. Если выбран "Отлично":
   - Возврат на предыдущий экран
   - Список бронирований обновится при следующем открытии

---

## 🔄 Синхронизация бронирований

### Автоматическая синхронизация:
- **Периодическая:** Каждые 30 минут
- **Webhook:** При изменениях в YClients (мгновенно)

### Что синхронизируется:
- Создание новой записи
- Изменение статуса записи
- Отмена записи
- Обновление даты/времени

### Сопоставление пользователей:
- По email
- По телефону (с нормализацией)

---

## 📝 Особенности реализации

### Предзаполнение формы:
- Данные пользователя автоматически передаются в URL виджета
- Пользователю не нужно вводить данные заново
- Улучшает UX и снижает ошибки

### Отслеживание успешного бронирования:
- Мониторинг изменений URL в WebView
- Проверка признаков успеха в URL
- Автоматическое определение завершения бронирования

### Обновление данных:
- После успешного бронирования обновляются списки
- Используется pull-to-refresh для ручного обновления
- Синхронизация с backend происходит автоматически

---

## 🛠️ Технический стек

**Frontend:**
- Flutter
- `webview_flutter` - для отображения YClients виджета
- `connectivity_plus` - для проверки интернета

**Backend:**
- FastAPI
- SQLAlchemy (PostgreSQL)
- YClients API интеграция
- APScheduler - для периодической синхронизации

**YClients:**
- Виджет для онлайн-записи
- API для получения записей
- Webhook для уведомлений

---

## ⚠️ Важные моменты

1. **Все бронирования через YClients:**
   - Нельзя забронировать услугу без YClients
   - Оплата происходит в виджете YClients
   - Не нужен отдельный экран оплаты

2. **Предзаполнение данных:**
   - Имя, email, телефон автоматически передаются
   - Пользователь может изменить данные в виджете

3. **Синхронизация:**
   - Записи из YClients автоматически синхронизируются
   - Можно синхронизировать вручную через API

4. **Обработка ошибок:**
   - Нет интернета → показывается `OfflineWidget`
   - Ошибка загрузки → `EmptyState` с кнопкой повтора
   - Детальные сообщения об ошибках

---

## 📌 Итого

**Основной путь бронирования:**
```
MenuSpaScreen → ServiceDetailScreen → YClientsBookingScreen → BookingsScreen
```

**Все происходит в YClients виджете:**
- Выбор мастера
- Выбор даты и времени
- Заполнение данных
- Оплата

**Наше приложение:**
- Показывает услуги
- Запускает виджет YClients
- Отслеживает успешное бронирование
- Показывает список бронирований
- Синхронизирует данные с YClients

