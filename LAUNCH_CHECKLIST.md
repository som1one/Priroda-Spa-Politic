# План подготовки к релизу PRIRODA SPA в Google Play и App Store

Этот файл — подробный чек‑лист, по которому можно идти сверху вниз.  
Цель: **успеть к 30‑му числу**, с запасом на возможные правки от сторов.

Рекомендуется распечатать / открыть рядом и ставить галочки.

---

## 0. Обозначения и структура

- ✅ — уже сделано (по коду/докам, но всё равно стоит проверять).
- 🔴 — критично до релиза (без этого лучше не отправлять).
- 🟡 — желательно до первого релиза, но можно часть перенести на обновление.
- 🟢 — можно сделать уже после выхода первой версии.

Структура:

1. [Общие принципы и приоритеты](#1-общие-принципы-и-приоритеты)
2. [Бэкенд и продакшен‑окружение](#2-бэкенд-и-продакшен-окружение)
3. [Аутентификация (email + Google)](#3-аутентификация-email--google)
4. [Бронирования и YClients](#4-бронирования-и-yclients)
5. [Push‑уведомления](#5-push-уведомления)
6. [Flutter‑приложение: подготовка к продакшену](#6-flutter-приложение-подготовка-к-продакшену)
7. [Деплой сервера и админ‑панели](#7-деплой-сервера-и-админ-панели)
8. [Google Play: шаг‑за‑шагом](#8-google-play-шаг-за-шагом)
9. [App Store: шаг‑за‑шагом](#9-app-store-шаг-за-шагом)
10. [Мини‑регресс перед релизом](#10-мирегресс-перед-релизом)
11. [Рекомендуемый план по дням](#11-рекомендуемый-план-по-дням)

---

## 1. Общие принципы и приоритеты

### 1.1. Что обязательно до релиза (🔴)

- 🔴 Стабильный **бэкенд в продакшене** (HTTPS, корректный `.env`, БД, миграции).
- 🔴 Рабочая **аутентификация**:
  - email + пароль;
  - вход через Google с реальным Google Client ID.
- 🔴 Надёжный флоу **бронирований**:
  - создание;
  - просмотр;
  - отмена;
  - корректная работа с YClients (минимум — не ломать и не врать пользователю).
- 🔴 **Push‑уведомления**:
  - хотя бы базовая отправка и получение;
  - отсутствие крэшей при получении/нажатии.
- 🔴 Минимально приличный UX:
  - нет крэшей на ключевых экранах;
  - есть политика конфиденциальности;
  - ошибки показываются по‑человечески.

### 1.2. Что можно перенести на обновление (🟡/🟢)

- 🟡 Offline‑режим, умный retry всех запросов.
- 🟡 Полноценная двусторонняя синхронизация с YClients (Webhook + cron).
- 🟡 Полный CI/CD, мониторинг, аналитика (Sentry, Amplitude и т.п.).
- 🟢 Дополнительные фичи (сложные акции, детальная аналитика, продвинутые отчёты).

---

## 2. Бэкенд и продакшен‑окружение

Работаем в директории `backend/`.

### 2.1. Настройка `.env` для продакшена (🔴)

1. Скопировать шаблон:

   ```bash
   cd backend
   copy env.template .env  # Windows
   # или
   cp env.template .env    # Linux/Mac
   ```

2. Открыть `.env` и выставить **боевые значения**:

   - 🔴 `ENVIRONMENT=production`
   - 🔴 `DEBUG=False`
   - 🔴 `SECRET_KEY=<длинная_случайная_строка_минимум_32_символа>`
   - 🔴 `DATABASE_URL=postgresql://user:password@<prod-host>:<port>/spa_db`
   - 🔴 `CORS_ORIGINS=https://api.ТВОЙ_ДОМЕН,https://admin.ТВОЙ_ДОМЕН`
   - 🟡 `REDIS_URL` и `REDIS_ENABLED=True`, если будешь использовать Redis.
   - 🔴 Email‑настройки (SMTP):
     - `SMTP_HOST`, `SMTP_PORT`
     - `SMTP_USERNAME`, `SMTP_PASSWORD`
     - `SMTP_FROM`
   - 🔴 `GOOGLE_CLIENT_ID=<боевой client_id>.apps.googleusercontent.com`
   - 🟡 `FCM_SERVER_KEY=<серверный ключ из Firebase>`
   - 🟡 `YCLIENTS_ENABLED=True/False` (см. раздел [4](#4-бронирования-и-yclients))

3. Проверить, что **секреты не захардкожены** в `app/core/config.py`.  
   Всё чувствительное должно браться из `.env`.

### 2.2. Миграции и запуск API (🔴)

1. Убедиться, что PostgreSQL доступен (через Docker или управляемую БД).
2. Применить миграции:

   ```bash
   cd backend
   alembic upgrade head
   ```

3. Запустить сервер в dev‑режиме (для проверки, не для прод):

   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

4. Проверить в браузере/через curl:

   - `GET /` → `{"status": "ok", ...}`
   - `GET /health` → `{"status": "healthy", ...}`
   - `GET /docs` → Swagger открывается.

5. Для продакшена использовать gunicorn:

   ```bash
   pip install gunicorn
   gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
   ```

---

## 3. Аутентификация (email + Google)

### 3.1. Стандартная аутентификация (email/пароль) (🔴)

**Бэкенд**

1. Убедиться, что эндпоинты работают:
   - `POST /api/v1/auth/register`
   - `POST /api/v1/auth/verify-email`
   - `POST /api/v1/auth/login`
   - `GET  /api/v1/auth/me`

2. Проверить последовательность:
   - регистрация пользователя;
   - получение кода (в ответе и/или по email);
   - подтверждение email кодом;
   - вход логином/паролем;
   - корректный JWT (access + refresh, если используется).

**Клиент (Flutter)**

1. В `spa/lib/services/auth_service.dart` убедиться:
   - `login(email, password)` отправляет запрос на `/auth/login` и сохраняет `access_token`;
   - `register(...)`:
     - сначала отправляет данные и код подтверждения;
     - после успешной регистрации пытается автоматически залогиниться;
   - `restoreSession()`:
     - при старте достаёт токен из `StorageService` и выставляет его в `ApiService`.

2. Протестировать сценарии:
   - новая регистрация → подтверждение → авто‑логин;
   - выход → повторный логин;
   - перезапуск приложения → пользователь всё ещё авторизован.

### 3.2. Google Sign‑In — клиент (Flutter + Firebase) (🔴)

Работаем в `spa/`.

1. Установить зависимости:

   ```bash
   cd spa
   flutter pub get
   ```

2. Проверить, что созданы и лежат на месте:
   - `android/app/google-services.json`
   - `ios/Runner/GoogleService-Info.plist`
   - `lib/firebase_options.dart` (генерируется через `flutterfire configure`)

3. Если чего‑то нет:
   - следовать `GOOGLE_SIGNIN_SETUP.md`:
     - создать проект Firebase;
     - добавить Android/iOS приложения;
     - скачать и положить файлы конфигурации;
     - выполнить:

       ```bash
       flutter pub global activate flutterfire_cli
       flutterfire configure
       ```

4. В `lib/main.dart` проверить инициализацию:
   - `Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform)` вызывается до `runApp`.
   - `FirebaseMessaging.onBackgroundMessage` зарегистрирован.

### 3.3. Google Sign‑In — бэкенд (🔴)

1. В `backend/.env`:

   ```env
   GOOGLE_CLIENT_ID=<боевой_client_id>.apps.googleusercontent.com
   ```

2. В `app/core/config.py`:
   - убрать хардкоженный `GOOGLE_CLIENT_ID` или оставить только дефолт;
   - убедиться, что значение берётся из `.env`.

3. На стороне Flutter (`AuthService`):
   - `serverClientId` в `_googleServerClientId` должен совпадать с `GOOGLE_CLIENT_ID` из `.env`.
   - При желании: сделать так, чтобы Client ID брался через конфиг, а не был захардкожен.

4. Прогнать сценарии:
   - Первый вход через Google:
     - открывается окно выбора аккаунта;
     - после выбора — создаётся пользователь в БД;
     - бэкенд возвращает JWT (как при обычном логине);
     - токен сохраняется на клиенте, сессия работает.
   - Повторный вход через Google:
     - повторно заходит без дублей пользователя;
     - токен обновляется.
   - Отмена входа:
     - приложение не падает, просто остаётся на экране логина.

---

## 4. Бронирования и YClients

### 4.1. Локальные бронирования (🔴)

Цель: пользователь всегда видит **свои записи**, созданные через приложение.

**Бэкенд**

1. Проверить эндпоинты:
   - `GET    /api/v1/bookings` — список бронирований текущего пользователя;
   - `POST   /api/v1/bookings` — создание новой записи;
   - `PUT    /api/v1/bookings/{id}` — изменение (в т.ч. отмена);
   - (опционально) `GET /api/v1/bookings/{id}` — детали.

2. Проверить логику статусов:
   - `pending` — ожидает подтверждения;
   - `confirmed` — подтверждено;
   - `completed` — завершено;
   - `cancelled` — отменено (в т.ч. пользователем).

**Клиент (Flutter)**

1. Экран `BookingsScreen` (`spa/lib/screens/bookings/bookings_screen.dart`):
   - загрузка бронирований через `ApiService.get('/bookings')`;
   - поддержка фильтров по статусам:
     - Все / Ожидают / Подтверждены / Завершены / Отменены;
   - `pull to refresh`:
     - при потягивании вниз вызывает повторную загрузку с `forceRefresh:true`.

2. Отмена бронирования:
   - кнопка «Отменить» отправляет `PUT /bookings/{id}` со статусом `cancelled`;
   - после успешного ответа:
     - показывается SnackBar «Бронирование отменено»;
     - список обновляется (повторный запрос).

3. Навигация:
   - из нижнего таба или меню есть переход на `BookingsScreen`;
   - после успешного создания брони (из меню услуг) при возврате:
     - либо экран получает аргумент `refreshBookings: true`,
     - либо явно перезагружает список.

### 4.2. Интеграция с YClients (🟡, но проверку сделать обязательно)

Задача: интеграция **не должна ломать** локальный флоу и вводить в заблуждение.

1. В `backend/.env`:
   - решить стратегию на первый релиз:
     - Вариант A (проще и надёжнее):  
       `YCLIENTS_ENABLED=False` — приложение работает как самостоятельное бронирование, без синхронизации; про YClients нигде явно не заявляем в описании приложения.
     - Вариант B (если интеграция уже стабильна):  
       `YCLIENTS_ENABLED=True` и заполнены:
       - `YCLIENTS_COMPANY_ID`
       - `YCLIENTS_API_TOKEN`
       - `YCLIENTS_USER_TOKEN`

2. Если включаешь вариант B:
   - проверить:
     - создание брони из приложения → запись появляется в интерфейсе YClients;
     - отмена / изменение статуса через приложение → ожидаемое поведение в YClients;
   - убедиться, что при временной недоступности YClients:
     - приложение не падает;
     - пользователь получает внятное сообщение (типа «Сервис временно недоступен»).

3. Полная двусторонняя синхронизация (webhook + периодические задачи):
   - 🟡 Можно вынести в следующий релиз:
     - обработка webhooks от YClients;
     - периодическое подтягивание последних записей;
     - консистентность статусов между системами.

---

## 5. Push‑уведомления

### 5.1. Настройка Firebase Cloud Messaging (🔴 для базового сценария)

1. В Firebase Console:
   - включить Cloud Messaging;
   - получить **Server key** и прописать в `backend/.env`:

     ```env
     FCM_SERVER_KEY=<серверный_ключ>
     ```

2. Убедиться, что `FCM_API_URL` в конфиге — `https://fcm.googleapis.com/fcm/send`.

### 5.2. Клиентская часть (Flutter) (🔴)

1. В `lib/main.dart`:
   - `FirebaseMessaging.onBackgroundMessage` зарегистрирован;
   - `PushService().init()` вызывается после инициализации Firebase и восстановления сессии.

2. В `PushService` (если есть):
   - запрос разрешений:
     - Android: каналы уведомлений;
     - iOS: `requestPermission(...)`.
   - получение и обновление FCM‑токена:
     - при первом запуске;
     - при обновлении токена.
   - отправка токена на бэкенд:
     - соответствующий эндпоинт (например, `/api/v1/push/register`).

3. Обработка уведомлений:
   - в foreground:
     - уведомление либо показывается силами клиента (in‑app баннер/диалог),
     - либо пользователь видит системное уведомление и может на него нажать.
   - в background / при закрытом приложении:
     - тап по пушу открывает приложение;
     - при наличии в payload информации (тип, id акции, id брони и т.п.) —
       происходит навигация на нужный экран (можно начать с самого простого сценария).

### 5.3. Бэкенд (🟡, базовый сценарий обязателен)

1. Эндпоинты:
   - регистрация токена устройства пользователя;
   - (опционально) отписка / обновление токена.

2. Логика отправки:
   - базовый сценарий: отправка пуша по событию:
     - создано бронирование;
     - подтверждено бронирование;
     - отмена бронирования.
   - в payload указать:
     - `title`, `body`;
     - (опционально) `type` и идентификатор сущности для навигации.

3. Тест:
   - получить реальный FCM‑токен для устройства;
   - отправить тестовый пуш через API бэка;
   - убедиться, что устройство его получает и корректно обрабатывает.

---

## 6. Flutter‑приложение: подготовка к продакшену

### 6.1. Общие настройки (🔴)

1. `SERVER_BASE_URL`:
   - в рантайме передавать:

     ```bash
     flutter run --dart-define=SERVER_BASE_URL=https://api.ТВОЙ_ДОМЕН
     flutter build apk --release --dart-define=SERVER_BASE_URL=https://api.ТВОЙ_ДОМЕН
     flutter build appbundle --release --dart-define=SERVER_BASE_URL=https://api.ТВОЙ_ДОМЕН
     ```

   - или прописать дефолт в `lib/utils/constants.dart` как `https://api.ТВОЙ_ДОМЕН` (для продсборок).

2. UI/UX:
   - проверить:
     - домашний экран (`HomeScreen`);
     - профиль;
     - программа лояльности;
     - меню услуг;
     - бронирования;
     - экран авторизации/регистрации.
   - убедиться, что нигде не видно raw‑ошибок или stacktrace; вместо этого — человеческие тексты.

3. Политика конфиденциальности и условия:
   - добавить экран/пункты меню:
     - «Политика конфиденциальности» → открывает страницу на сайте;
     - «Пользовательское соглашение» (по возможности).

### 6.2. Интеграции и ассеты (🟡)

1. Иконки:
   - для Android (`mipmap-*`);
   - для iOS (AppIcon).
2. Splash‑экраны:
   - убедиться, что на обоих платформах выглядят корректно.
3. Локализация:
   - проверка русской версии (основной);
   - при необходимости — корректность английских текстов (минимальный уровень).

---

## 7. Деплой сервера и админ‑панели

### 7.1. Сервер (VPS, Docker, nginx) (🔴)

1. Поднять VPS (Ubuntu 22.04, например).
2. Установить:
   - Docker и Docker Compose;
   - nginx;
   - certbot (для Let’s Encrypt).

3. Склонировать репозиторий на сервер:

   ```bash
   git clone <repo-url> /opt/spa
   cd /opt/spa/backend
   ```

4. Создать и заполнить `backend/.env` (см. раздел 2.1).
5. Поднять PostgreSQL:
   - либо через `docker-compose up -d` (если используешь локальный контейнер),
   - либо использовать управляемую БД и указать её в `DATABASE_URL`.
6. Применить миграции и запустить API под gunicorn (как в разделе 2.2).

### 7.2. HTTPS и домены (🔴)

1. Настроить DNS:
   - `api.ТВОЙ_ДОМЕН` → IP сервера;
   - `admin.ТВОЙ_ДОМЕН` → тот же IP.

2. Настроить nginx:
   - proxy_pass `https://api...` → `http://127.0.0.1:8000`;
   - `https://admin...` → директорию со статикой админки.

3. Настроить HTTPS:
   - через `certbot --nginx`;
   - автоматическое обновление сертификатов (cron/systemd‑timer).

### 7.3. Admin‑панель (🟡)

1. В `admin/.env`:

   ```env
   VITE_API_BASE_URL=https://api.ТВОЙ_ДОМЕН/api/v1
   ```

2. Собрать:

   ```bash
   cd admin
   npm install
   npm run build
   ```

3. Скопировать содержимое `admin/dist` в директорию, которую раздаёт nginx для `admin.ТВОЙ_ДОМЕН`.

---

## 8. Google Play: шаг‑за‑шагом

### 8.1. Подготовка релизного Android‑билда (🔴)

1. Создать/подготовить keystore (если ещё нет).
2. Прописать настройки подписи в `android/app` (`key.properties` и `build.gradle.kts`).
3. Добавить SHA‑1 **релизного** ключа в Firebase (для корректной работы Google Sign‑In).
4. Собрать релизный бандл:

   ```bash
   cd spa
   flutter build appbundle --release --dart-define=SERVER_BASE_URL=https://api.ТВОЙ_ДОМЕН
   ```

5. Протестировать релизный билд (APK или AAB) на реальном устройстве:
   - авторизация (email + Google);
   - бронирования;
   - push‑уведомления;
   - работа с продовским API.

### 8.2. Настройка Google Play Console (🔴)

1. Создать приложение:
   - язык: русский (и английский, при необходимости);
   - название;
   - краткое и полное описание.

2. Загрузить:
   - иконки;
   - скриншоты (несколько экранов приложения);
   - дизайн баннера (если требуется).

3. Заполнить:
   - контактный email;
   - URL сайта;
   - URL политики конфиденциальности.

4. Пройти:
   - опрос по контенту (Content rating);
   - форму о данных пользователя (Data safety);
   - указать целевую аудиторию (скорее всего взрослые пользователи).

5. Загрузить AAB и:
   - сначала отправить во внутренний тест (Internal testing);
   - при необходимости — закрытое/открытое тестирование;
   - затем — в продакшен (Production).

---

## 9. App Store: шаг‑за‑шагом

### 9.1. Подготовка к iOS‑релизу (🔴 при выпуске на iOS)

1. Иметь активную подписку Apple Developer.
2. В App Store Connect:
   - создать приложение;
   - указать Bundle ID (должен совпадать с `PRODUCT_BUNDLE_IDENTIFIER` в `ios/Runner`).

3. В Xcode:
   - открыть `ios/Runner.xcworkspace`;
   - настроить Team и Signing;
   - включить Push Notifications и Background Modes (Remote notifications).

4. Убедиться, что:
   - `GoogleService-Info.plist` добавлен в проект;
   - URL‑схемы Google Sign‑In прописаны (через `REVERSED_CLIENT_ID`).

### 9.2. Сборка и отправка билда

1. Собрать релизную версию:
   - через Xcode → Product → Archive, либо:

     ```bash
     cd spa
     flutter build ios --release --dart-define=SERVER_BASE_URL=https://api.ТВОЙ_ДОМЕН
     ```

2. Через Xcode Organizer / Transporter загрузить билд в App Store Connect.

### 9.3. Заполнение информации в App Store Connect

1. Метаданные:
   - название, подзаголовок;
   - описание (русский/английский);
   - ключевые слова.

2. Медиа:
   - скриншоты под основные устройства (iPhone 6.7", 6.1" и т.д.).

3. Политика и конфиденциальность:
   - указать URL политики конфиденциальности;
   - заполнить App Privacy (какие данные собираются и как используются).

4. Отправка на ревью:
   - в примечаниях к ревью можно коротко описать:  
     «Приложение для SPA‑салона с онлайн‑бронированием, программой лояльности и push‑уведомлениями. Доступна авторизация по email/паролю и через Google».

---

## 10. Мини‑регресс перед релизом

Перед тем как нажать «Отправить на проверку» в сторах, пройти один раз **полный сценарий** на **боевом сервере** и **релизных билдах**:

1. **Регистрация и логин**
   - [ ] Регистрация по email, ввод кода, первый вход.
   - [ ] Выход и повторный вход.
   - [ ] Восстановление сессии после перезапуска приложения.
   - [ ] Вход через Google (первый и повторный).

2. **Бронирования**
   - [ ] Создание брони из приложения.
   - [ ] Появление этой брони в списке `Мои записи`.
   - [ ] Фильтры по статусам.
   - [ ] Отмена бронирования и обновление статуса.
   - [ ] (Если включен YClients) Отражение записи в интерфейсе YClients.

3. **Push‑уведомления**
   - [ ] Получение пуша в foreground.
   - [ ] Получение и тап по пушу в background/после закрытия.
   - [ ] Отсутствие крэшей при обработке.

4. **Навигация и UX**
   - [ ] Домашний экран, профиль, программа лояльности, меню, бронирования — без падений.
   - [ ] Все внешние ссылки (сертификаты, товары, сайт, политика) корректно открываются.

5. **Сервер**
   - [ ] `/health` и `/docs` доступны.
   - [ ] В логах нет явных ошибок при типичных сценариях.

---

## 11. Рекомендуемый план по дням

Приблизительный план, чтобы **успеть к 30‑му с запасом** (можно сдвигать по факту).

### День 1–2

- Бэкенд:
  - [ ] Настроить `backend/.env` под прод.
  - [ ] Запустить API на сервере.
  - [ ] Проверить миграции и основные эндпоинты.
- Аутентификация (email/пароль):
  - [ ] Глубокий прогон регистрации/логина.

### День 3–4

- Google Sign‑In:
  - [ ] Завершить настройку Firebase и Google Sign‑In (Android + iOS).
  - [ ] Связать Flutter и бэкенд по Client ID.
  - [ ] Прогнать все сценарии входа с Google.

### День 5–6

- Бронирования:
  - [ ] Добить `BookingsScreen` и его взаимодействие с API.
  - [ ] Пройти флоу: создание → просмотр → отмена.
- YClients:
  - [ ] Принять решение: включён/выключен на релиз.
  - [ ] Если включён — протестировать на боевом окружении.

### День 7–8

- Push‑уведомления:
  - [ ] Настроить FCM (серверный ключ, регистрация токенов).
  - [ ] Реализовать базовую отправку пушей по ключевым событиям.
  - [ ] Проверить поведение на реальном устройстве.

### День 9–10

- Flutter‑приложение:
  - [ ] Проверить/доработать иконки, splash, тексты, политику.
  - [ ] Подготовить скриншоты для стор.
- Сервер/админка:
  - [ ] Настроить nginx + HTTPS для API и админ‑панели.

### День 11–12

- Google Play:
  - [ ] Собрать релизный AAB.
  - [ ] Настроить Google Play Console.
  - [ ] Отправить на ревью (лучше сделать это как можно раньше).

### День 13–14

- App Store (если релизишь iOS сразу):
  - [ ] Собрать iOS‑релиз.
  - [ ] Заполнить метаданные и отправить на ревью.
- Мини‑регресс:
  - [ ] Пройти ещё раз все критичные сценарии.

---

Если что‑то из этого не успеваешь к дате — **в первую очередь** должны быть стабильны:

- Бэкенд (HTTPS, БД, безопасность).
- Аутентификация.
- Бронирования.
- Базовые push‑уведомления.

Остальное можно докрутить в 1–2 обновлениях после выхода.  
Этот файл можно дополнять своими заметками/датами по мере продвижения.


