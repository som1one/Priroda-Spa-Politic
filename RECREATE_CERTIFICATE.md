# 🔐 Пересоздание сертификата с новым паролем

## Новый пароль: `prirodaspa2018`

### ⚠️ Проблема: OpenSSL не установлен

Для пересоздания `.p12` нужен OpenSSL. У вас два варианта:

---

## ✅ Вариант 1: Обновить только пароль в GitHub (РЕКОМЕНДУЕТСЯ)

Если у вас уже есть рабочий `.p12`, просто обновите пароль в секрете:

1. **GitHub → Settings → Security → Secrets and variables → Actions**
2. Найдите `APPLE_CERTIFICATE_PASSWORD`
3. Нажмите на иконку редактирования
4. Введите новый пароль: `prirodaspa2018`
5. **Update secret**

**Готово!** Workflow будет использовать новый пароль.

---

## ✅ Вариант 2: Пересоздать .p12 (нужен OpenSSL)

Если нужно пересоздать `.p12` с новым паролем:

### Шаг 1: Установите OpenSSL

См. `INSTALL_OPENSSL_WINDOWS.md` (если файл удален, скачайте с https://slproweb.com/products/Win32OpenSSL.html)

### Шаг 2: Пересоздайте .p12

```powershell
# Конвертируйте .cer в .pem
openssl x509 -inform DER -in ios_distribution.cer -out certificate.pem

# Создайте .p12 с новым паролем
openssl pkcs12 -export -out certificate.p12 -inkey private_key.pem -in certificate.pem -password pass:prirodaspa2018

# Конвертируйте в base64
[Convert]::ToBase64String([IO.File]::ReadAllBytes("certificate.p12")) | Out-File -Encoding ASCII certificate_base64.txt
```

### Шаг 3: Обновите секреты в GitHub

1. `APPLE_CERTIFICATE_BASE64` - новый base64 из `certificate_base64.txt`
2. `APPLE_CERTIFICATE_PASSWORD` - `prirodaspa2018`

---

## 🎯 Рекомендация

**Используйте Вариант 1** - просто обновите пароль в GitHub Secrets. Это проще и быстрее.

Если `.p12` уже работает, не нужно его пересоздавать - просто обновите пароль в секрете.

