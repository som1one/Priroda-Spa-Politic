# ✅ Проверка переменных в Codemagic

## 🔥 Критически важно: Названия переменных

В группе `app_store_credentials` переменные должны называться **ТОЧНО** так:

### ✅ Правильные названия:

1. **`APP_STORE_ISSUER_ID`**
   - Значение: `4fbfcedf-2756-4b8e-8fc3-b17978e9532a` (ваш Issuer ID)

2. **`APP_STORE_KEY_ID`**
   - Значение: `84SR375827` (ваш Key ID)

3. **`APP_STORE_PRIVATE_KEY`**
   - Значение: Содержимое `.p8` файла (весь текст, включая `-----BEGIN PRIVATE KEY-----` и `-----END PRIVATE KEY-----`)

---

## ⚠️ Если названия отличаются

Если хотя бы одно название отличается (например `APPLE_KEY_ID` вместо `APP_STORE_KEY_ID`):
- ❌ Codemagic НЕ найдёт данные
- ❌ Code signing не будет работать
- ❌ Сборка упадёт с ошибкой

---

## 🔍 Как проверить в Codemagic

### Шаг 1: Откройте Environment Variables

1. **Codemagic → Ваш проект → Settings**
2. **Environment variables**
3. Найдите группу **`app_store_credentials`**

### Шаг 2: Проверьте названия

Убедитесь, что переменные называются **ТОЧНО**:

- ✅ `APP_STORE_ISSUER_ID` (не `APPLE_ISSUER_ID`, не `ISSUER_ID`)
- ✅ `APP_STORE_KEY_ID` (не `APPLE_KEY_ID`, не `KEY_ID`)
- ✅ `APP_STORE_PRIVATE_KEY` (не `APPLE_PRIVATE_KEY`, не `PRIVATE_KEY`)

### Шаг 3: Проверьте значения

1. **`APP_STORE_ISSUER_ID`:**
   - Должно быть: `4fbfcedf-2756-4b8e-8fc3-b17978e9532a`

2. **`APP_STORE_KEY_ID`:**
   - Должно быть: `84SR375827`

3. **`APP_STORE_PRIVATE_KEY`:**
   - Должно быть содержимое `.p8` файла (весь текст)
   - Начинается с: `-----BEGIN PRIVATE KEY-----`
   - Заканчивается: `-----END PRIVATE KEY-----`

---

## 🔧 Если названия неправильные

### Исправьте названия:

1. **Удалите старые переменные** с неправильными названиями
2. **Создайте новые** с правильными названиями:
   - `APP_STORE_ISSUER_ID`
   - `APP_STORE_KEY_ID`
   - `APP_STORE_PRIVATE_KEY`
3. **Скопируйте значения** из старых переменных
4. **Сохраните**

---

## ✅ Проверка в codemagic.yaml

В `codemagic.yaml` вы используете:

```yaml
groups:
  - app_store_credentials
```

Это правильно! Codemagic будет искать переменные с названиями:
- `APP_STORE_ISSUER_ID`
- `APP_STORE_KEY_ID`
- `APP_STORE_PRIVATE_KEY`

---

## 🎯 Чек-лист

Перед запуском сборки убедитесь:

- [ ] Группа называется: `app_store_credentials`
- [ ] Переменная 1: `APP_STORE_ISSUER_ID` = `4fbfcedf-2756-4b8e-8fc3-b17978e9532a`
- [ ] Переменная 2: `APP_STORE_KEY_ID` = `84SR375827`
- [ ] Переменная 3: `APP_STORE_PRIVATE_KEY` = содержимое `.p8` файла
- [ ] Все переменные типа **Secure** (зашифрованные)
- [ ] Названия написаны **ТОЧНО** как указано выше

---

**Главное:** Названия переменных должны быть **ТОЧНО** `APP_STORE_ISSUER_ID`, `APP_STORE_KEY_ID`, `APP_STORE_PRIVATE_KEY` - без изменений!

